% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remediacion.R
\name{planificar_limpieza}
\alias{planificar_limpieza}
\alias{aplicar}
\title{Construir y aplicar un plan de limpieza auditable}
\usage{
planificar_limpieza(perfil, datos = NULL, soporte_minimo_dependencia = 2L)

aplicar(plan, datos, permitir_eliminacion = FALSE, conservar_eliminados = TRUE)
}
\arguments{
\item{perfil}{Objeto de clase \code{perfil} creado por \code{\link[=perfilar]{perfilar()}}.}

\item{datos}{\code{data.frame}, \code{tibble} o \code{data.table} sobre el que se ejecuta el
plan. El objeto recibido no se modifica.}

\item{soporte_minimo_dependencia}{Cantidad mínima de observaciones
concordantes por valor determinante para proponer una imputación.}

\item{plan}{Objeto de clase \code{plan_limpieza} o data frame con el mismo
contrato. Puede filtrarse y editarse antes de aplicarlo.}

\item{permitir_eliminacion}{Segundo consentimiento obligatorio para ejecutar
acciones que eliminan filas o columnas.}

\item{conservar_eliminados}{Si se conservan en el resultado las filas y
columnas retiradas. Es \code{TRUE} de forma predeterminada.}
}
\value{
\code{planificar_limpieza()} devuelve un data frame de clase
\code{plan_limpieza}. \code{aplicar()} devuelve una lista de clase
\code{resultado_limpieza} con \code{datos}, \code{registro}, \code{plan_aplicado}, el \code{plan}
sincronizado y \code{eliminados}. El \code{registro} conserva \code{estado} (\code{ejecutada}
o \code{fallida}), \code{error}, \code{n_no_reversibles} y la \code{justificacion} de cada
acción seleccionada, incluso cuando una falla y las siguientes continúan.
\code{n_no_reversibles} cuenta las celdas cuyo VALOR se perdió y no se puede
recuperar desde el resultado: un centinela \code{-999} que pasa a ausencia, un
extremo recortado a su límite, un marcador de ausencia convertido, o un
número que se redondeó al convertirlo. No cuenta los cambios de forma que
dejan el valor en su lugar —recortar espacios, reemplazar separadores o
cambiar mayúsculas—, aunque tampoco se puedan deshacer tal cual.
Si una acción seleccionada no produce ningún efecto, se registra como
\code{fallida} con el motivo y su copia no se incorpora al resultado. La
comprobación del efecto observa \code{n_cambiadas} aunque \code{n_afectadas} sea
\code{NA} o haya sido editado: una acción seleccionada que no cambia nada queda
\code{fallida}, porque la ausencia de efecto es observable sin depender de la
estimación.
Si una columna de entrada es un factor, las acciones que transforman su
texto devuelven una columna \code{character}: no se reconstruyen los niveles
originales, porque una limpieza puede introducir valores nuevos.
}
\description{
\code{planificar_limpieza()} transforma los hallazgos de un objeto \code{perfil} en un
objeto de datos editable, sin modificar los datos examinados. Cada fila
representa una acción propuesta. Sólo se marcan como recomendadas las
estrategias correctas con independencia del dominio; algunas, como marcar
valores extremos, permanecen inactivas hasta que se decida actuar. Las
decisiones contextuales quedan desactivadas y los formatos de fecha ambiguos
quedan bloqueados.
}
\details{
\code{aplicar()} ejecuta exclusivamente las filas con \code{aplicar == TRUE}, sobre una
copia de \code{datos}. Verifica que cada columna siga siendo identificable y que
las conversiones sean completas antes de sustituirla. Devuelve los datos
nuevos junto con un registro de las acciones y sus parámetros. El mismo
registro queda en el atributo \code{registro_limpieza} de los datos resultantes.

Las alternativas para un mismo hallazgo comparten \code{grupo}; las acciones
independientes usan \code{NA}. Como máximo una alternativa de cada grupo puede
tener \code{aplicar == TRUE}, invariante que \code{aplicar()} vuelve a validar. No se
agrega una fila ficticia para "no hacer nada": \code{decision_grupo} distingue
\code{pendiente}, \code{recomendada}, \code{desactivada}, \code{elegida} y \code{omitida}, mientras
\code{recomendacion_grupo = "no_hacer_nada"} representa una recomendación
explícita de conservar los datos. Esto permite separar un grupo aún no
revisado de una omisión deliberada.

\code{estado} distingue acciones \code{lista}, \code{bloqueada} e \code{informativa}; \code{orden}
fija la secuencia reproducible. Si dos acciones comparten el mismo \code{orden},
el empate lo resuelve \code{id_accion}, no la posición de la fila: reordenar el
plan no cambia el resultado. Y un grupo marcado como \code{elegida} sin ninguna
acción activa se rechaza, porque no es ni una elección ni una omisión. \code{n_afectadas} es la estimación del perfil
sobre \strong{lo que esta acción tocaría}, que puede ser menos que el conteo del
hallazgo que la originó cuando la acción sólo cubre parte del caso: una
columna con tres valores de codificación rota, de los cuales uno es
reparable, produce un hallazgo con \code{n_afectados = 3} y una acción
\code{reparar_codificacion} con \code{n_afectadas = 1}. Las dos cifras son ciertas y
cuentan cosas distintas. \code{unidad_conteo} dice si cuenta filas, columnas o
valores distintos —lo
declara la acción cuando cuenta en una unidad propia, y sólo si no lo hace se
hereda del hallazgo—. El registro informa \code{n_cambiadas} sobre los datos
recibidos.

\strong{\code{n_afectadas} y \code{n_cambiadas} pueden no coincidir, y las dos son ciertas.}
La estimación se calcula sobre los datos que se perfilaron; el registro
cuenta lo que pasó al aplicar. Si una acción anterior del mismo plan ya tocó
esa columna, la posterior encuentra menos —o más— de lo estimado: con
\code{convertir_sentinelas_numericos} (orden 110) convirtiendo tres \code{-999} en
ausentes, \code{winsorizar_outliers} (orden 520) recorta dos valores donde el plan
estimaba siete. No es un desvío que ocultar: \code{orden}, \code{n_afectadas} y
\code{n_cambiadas} se publican los tres, y compararlos es la forma de ver el
efecto de la composición. Sólo el caso extremo —la acción no produce \strong{ningún}
efecto —también cuando el plan no trae una estimación válida— se registra
como \code{fallida} con su motivo.

\strong{Esa comparación sólo lee composición cuando las dos cifras cuentan en la
misma unidad.} \code{n_afectadas} cuenta en la \code{unidad_conteo} que el plan
declara y \code{n_cambiadas} cuenta lo que la acción tocó al aplicarse, que es
su unidad natural. Cuando la acción declara \code{unidad_conteo = "valor_distinto"} —las conversiones de mayúsculas y minúsculas—, las dos
cifras miden poblaciones distintas y su diferencia no dice nada sobre la
composición: medido sobre \code{c("Ana", "ana", "ANA", "Beto", " Ana ")},
\code{convertir_minusculas} estima \code{n_afectadas = 3} valores distintos y el
registro publica \code{n_cambiadas = 4} celdas. Las dos son ciertas. La unidad
del plan se recupera uniendo el registro con el plan por \code{id_accion}, que
los dos publican. \code{reversible}
indica si la conversión conserva la identidad de cada valor. Las
conversiones se comprueban sobre todos los valores de \code{datos}: las numéricas
bloquean ceros iniciales, colisiones no inyectivas y valores con más cifras
significativas de las que guarda un número de doble precisión —un decimal se
compara con la precisión con que vino escrito—, mientras que fechas,
fechas-hora y lógicos sólo bloquean conversiones no ejecutables o no
inyectivas. Las fechas pueden cambiar a la representación canónica del tipo
sin que eso sea una pérdida. Sin \code{datos} no se puede hacer la comprobación y
la acción queda bloqueada. Cuando \strong{una conversión de tipo} se ejecuta y no
es reversible se marca \code{destructiva} —también si la columna no era segura y
se activa a mano—, no se activa por defecto y el registro conserva
\code{n_no_reversibles} y la justificación de la decisión.

\code{destructiva} no es sinónimo de "pierde algo": marca las acciones que el
usuario tiene que activar a mano —las que retiran filas o columnas y las
conversiones que pierden representación— y por eso ninguna acción
\code{destructiva} puede estar \code{recomendada}. Hay acciones recomendadas que sí
pierden el valor de una celda: \code{convertir_ausencias_textuales} cambia un
marcador por \code{NA} y \code{eliminar_controles_invisibles} quita un carácter. Esas
lo dicen en su justificación y el registro las cuantifica en
\code{n_no_reversibles}; leer \code{destructiva = FALSE} no significa que no se haya
perdido nada, sino que el paquete pudo recomendar la acción sin conocer el
dominio.
Sobre una columna \strong{factor} las acciones por celda devuelven texto: el
resultado no puede ser un factor incompleto, así que el orden declarado y los
niveles sin observaciones no se conservan. La justificación de la acción lo
dice, y si el factor es ordenado la acción queda recomendada pero \strong{sin
activar}, porque conservar el orden es una decisión del dominio.

Dos atributos del plan declaran lo que el plan no cubre.
\code{cobertura_diagnosticos} trae los diagnósticos que el perfil no pudo
evaluar, para que leer tres acciones no se confunda con "lo demás está
bien". \code{hallazgos_sin_accion_por_columna_ambigua} trae los hallazgos medidos
que no produjeron acción porque su columna comparte nombre con otra y no hay
forma de saber sobre cuál actuaría la limpieza; el remedio es normalizar los
nombres y volver a perfilar. Los dos se imprimen con el plan.

\code{convertir_numero_regional} sólo se recomienda si todos los valores
presentes comparten convención decimal, unidad y moneda. Un valor con \verb{\%}
se divide por 100 y queda como proporción en \verb{[0, 1]}, la escala con que el
paquete publica toda proporción; una unidad o un símbolo de moneda dejan de
formar parte del valor y quedan en \code{parametros}. La justificación lo dice
cuando ocurre. Una columna que mezcla \code{5} con \verb{5 \%} no se convierte: habría
que decidir si el número sin \verb{\%} está en la misma escala.
La acción de codificación prueba las tablas congeladas de varias
codificaciones y deja en \code{estado_reparacion} uno de \code{reparado},
\code{reparado_parcialmente} o \code{no_se_pudo}. Una reparación parcial no se activa
automáticamente: debe revisarse y seleccionarse de forma explícita. La
estrategia se llama \code{reparar_codificacion} y no limita el motor a latin-1.
Unos mismos bytes pueden recibir dos diagnósticos: \verb{c2 80} es a la vez un
carácter de control C1 y la huella de un \verb{€} de Windows-1252 leído como
latin-1. Cuando el plan propone reparar la codificación y eliminar ese
control, manda la reparación —corre primero y restituye el \verb{€}—, y la
eliminación queda sin nada que hacer. Es la lectura habitual en texto real;
en el caso raro de un control C1 genuino, la reparación lo convierte en \verb{€}.
Una celda cuyo texto no se puede leer —declarada \code{bytes} con bytes que no
son UTF-8, o sin marca en una sesión UTF-8, que es lo que deja \code{\link[=read.csv]{read.csv()}}
sin \code{fileEncoding} sobre un archivo latin1— no se transforma: el perfil no la
midió y la informa como \code{codificacion_invalida}, cuyo remedio es volver a leer
la fuente declarando su codificación. Las acciones de texto trabajan sobre las
demás celdas de la columna y cuentan sólo lo que cambiaron.
Si se marca una acción que no está \code{lista}, \code{aplicar()} aborta antes de
modificar la copia y enumera las filas problemáticas. Una acción que sí está
lista pero falla se registra con su error y no impide aplicar las siguientes:
cada una conserva atomicidad sobre su propia columna o tabla. Las acciones
que efectivamente eliminan filas o columnas requieren además
\code{permitir_eliminacion = TRUE}; una conversión \code{destructiva} requiere selección
explícita y deja la pérdida cuantificada. Por defecto, el resultado conserva
lo retirado en \code{eliminados}; use \code{conservar_eliminados = FALSE} para evitar
ese costo de memoria.

Los hallazgos \code{controles_invisibles}, \code{entidades_html} y \code{separadores_en_campo}
tienen acciones separadas. La detección de invisibles informa tanto los
caracteres que se pueden normalizar como los ZWJ/ZWNJ significativos; la
normalización actúa sobre un conjunto más pequeño que la detección. La acción
\code{eliminar_controles_invisibles} quita controles C0/C1 que no son separadores
y los invisibles Unicode de transporte, y se recomienda por defecto; conserva
ZWJ/ZWNJ. \code{normalizar_espacios_invisibles} colapsa espacios Unicode (incluido
NBSP) a un espacio ASCII, no se recomienda por defecto y registra la pérdida
de reversibilidad. \code{decodificar_entidades_html} cubre las entidades
con nombre comunes en español y referencias numéricas válidas, pero no se
activa sola porque un ampersand puede ser contenido legítimo.
\code{reemplazar_separadores} convierte tabulaciones, saltos de línea, avances de
página y tabulaciones verticales (\verb{\\\\t}, \verb{\\\\n}, \verb{\\\\r}, \verb{\\\\r\\\\n}, \verb{\\\\f} y \verb{\\\\v})
en un espacio y también requiere una decisión explícita. Las tres acciones
registran el número de valores cambiados. Una comparación aproximada con
\code{normalizar = TRUE} usa estas mismas clases: colapsa espacios y omite basura
de transporte, pero conserva ZWJ/ZWNJ.

Las imputaciones por dependencia funcional se ofrecen desactivadas. Aunque
una dependencia exacta permite deducir un valor sin usar media, moda o un
modelo externo, sigue siendo una regularidad aprendida de una sola entrega y
puede reflejar un error sistemático en vez de una regla de negocio. El plan
conserva el mapa y su soporte para que el usuario la confirme; sólo entonces
se aplica y se vuelve a validar contra los datos recibidos. Si la protección
enmascaró alguna clave del mapa, éste no se usa como tabla de cruce: la
relación se reconstruye sobre los datos recibidos con el soporte declarado,
sin publicar sus valores.

\code{marcar_filas_duplicadas} añade dos columnas. \code{.fila_duplicada} reproduce la
semántica de \code{\link[=duplicated]{duplicated()}} y marca sólo las apariciones posteriores;
\code{.grupo_duplicado} identifica a \strong{todas} las filas que participan en cada
grupo de contenido idéntico. Marcar no elimina filas: con las dos columnas
incluidas, un perfil posterior tampoco vuelve a contar esas filas como
duplicadas exactas, porque las marcas las distinguen. Para saber si los
duplicados siguen en la tabla hay que quitar las columnas de marca antes de
perfilarla.

El orden operativo se aparta deliberadamente de la secuencia dimensional
frescura–completitud–exactitud–consistencia–unicidad sugerida por el marco.
Primero marca duplicados sin borrar, luego normaliza ausencias y texto, y
deja los cambios de esquema para el final. Esto evita perder la evidencia
original, permite imputar antes de convertir tipos y mantiene identificables
las columnas durante todo el plan. Las eliminaciones nunca se activan por
defecto, por lo que deduplicar temprano no puede hacer desaparecer registros.
\code{destructiva} también marca una conversión que pierde representación, aunque
no elimine filas o columnas. El consentimiento \code{permitir_eliminacion} sólo
se exige para las estrategias que efectivamente retiran filas o columnas;
una conversión destructiva requiere que el usuario la active explícitamente
y deja su pérdida cuantificada en el registro.

\strong{Marcar o eliminar ausentes va después de normalizarlos}, no antes. Una
columna con \code{"N/A"} y \code{"sin dato"} tiene ausentes disfrazados que
\code{convertir_ausencias_textuales} convierte en \code{NA} reales; si la marca
corriera primero, quedaría una columna \code{.ausente_x} que dice \code{FALSE} en
filas que terminan en \code{NA} —una afirmación falsa sobre los datos, publicada
con las dos acciones en \code{ejecutada} y sin error—. Medido sobre siete formas
de tabla antes de corregirlo, tres producían esa marca falsa siguiendo el
idioma documentado \code{plan$aplicar <- plan$recomendada}. Por la misma razón,
\code{eliminar_filas_ausentes} también va después: eliminar antes deja sin
eliminar las filas cuyo ausente todavía estaba disfrazado.
}
\examples{
datos <- data.frame(categoria = c(" A", "S/D", "B"))
perfil <- perfilar(datos)
plan <- planificar_limpieza(perfil, datos)
plan[, c("grupo", "estrategia", "recomendada", "aplicar")]
resultado <- aplicar(plan, datos)
resultado$datos
}
\seealso{
\code{\link[=perfilar]{perfilar()}}, \code{\link[=guiar_limpieza]{guiar_limpieza()}}, \code{\link[=detectar_dependencias]{detectar_dependencias()}}
}
