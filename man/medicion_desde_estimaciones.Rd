% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimaciones-externas.R
\name{medicion_desde_estimaciones}
\alias{medicion_desde_estimaciones}
\title{Llevar estimaciones ya calculadas al contrato de medición}
\usage{
medicion_desde_estimaciones(
  estimaciones,
  entidad,
  fuente,
  atributo = NULL,
  columnas = NULL,
  fecha = Sys.time()
)
}
\arguments{
\item{estimaciones}{Data frame con una fila por estimación y una columna por
estadístico. Los nombres reconocidos son los de
\code{\link[=estadisticos_estimacion]{estadisticos_estimacion()}}; se puede renombrar con \code{columnas}.}

\item{entidad}{Nombre de la entidad estimada, por ejemplo el tabulado o la
población de referencia.}

\item{fuente}{Texto que declara quién calculó las estimaciones. Es
obligatorio: sin él, el resultado no dice de dónde viene.}

\item{atributo}{Columna opcional de \code{estimaciones} que nombra el atributo o
la celda estimada. Cuando falta, se numeran las filas.}

\item{columnas}{Vector con nombres para traducir columnas de \code{estimaciones} a
estadísticos reconocidos, en la forma \code{c(cv = "coef_var")}.}

\item{fecha}{Fecha y hora de la medición.}
}
\value{
Data frame \code{medicion_calidad} con el contrato de \code{\link[=medir]{medir()}}, más las
columnas \code{fuente} y \code{unidad}.
}
\description{
\code{lupa} \strong{no estima}: eso necesita un diseño muestral, estimación de varianza
y otra disciplina. Lo que sabe hacer es evaluar contra un marco declarado.
Esta función recibe estimaciones calculadas por otra herramienta —\code{survey},
el paquete \href{https://github.com/inesscc/calidad}{\code{calidad}} del INE de Chile,
o cualquier otra— y las convierte en una medición que \code{\link[=evaluar]{evaluar()}} entiende.
}
\details{
Cada estadístico se convierte en \strong{una medida canónica propia}, con su
métrica, su tipo y su orientación, porque los siete tienen unidades y
dominios distintos: un coeficiente de variación y un tamaño de muestra no se
evalúan con la misma regla. Los reconocidos están en
\code{\link[=estadisticos_estimacion]{estadisticos_estimacion()}}.

Cada medida declara su procedencia en \code{fuente}, de modo que nadie lea el
resultado como si \code{lupa} lo hubiera calculado. Los estadísticos que la tabla
no traiga simplemente no producen medidas: no se rellenan con ceros ni se
estiman.
}
\examples{
estimaciones <- data.frame(
  celda = c("Montevideo", "Interior"),
  stat = c(0.42, 0.38),
  cv = c(0.08, 0.34),
  n = c(1200L, 90L)
)
medicion_desde_estimaciones(
  estimaciones, entidad = "ech2024", atributo = "celda",
  fuente = "survey 4.4, diseno complejo declarado por el equipo"
)
}
\seealso{
\code{\link[=estadisticos_estimacion]{estadisticos_estimacion()}}, \code{\link[=evaluar]{evaluar()}}, \code{\link[=marco_cepal]{marco_cepal()}}
}
