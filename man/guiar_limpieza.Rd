% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remediacion.R
\name{guiar_limpieza}
\alias{guiar_limpieza}
\title{Revisar decisiones de limpieza paso a paso}
\usage{
guiar_limpieza(
  plan,
  datos,
  selector = NULL,
  diccionarios = list(),
  max_ejemplos = 5L
)
}
\arguments{
\item{plan}{Objeto \code{plan_limpieza}.}

\item{datos}{Datos correspondientes al perfil que originó el plan.}

\item{selector}{Función opcional que recibe una lista con \code{grupo},
\code{acciones}, \code{elegibles}, \code{ejemplos} y \code{opciones}. Debe devolver la posición,
el identificador o el nombre de una estrategia, o \code{0} para no hacer nada.}

\item{diccionarios}{Lista opcional con nombre de diccionarios por columna.}

\item{max_ejemplos}{Máximo de ejemplos reales mostrados por grupo.}
}
\value{
El mismo \code{plan_limpieza} recibido, con la columna de decisión
sincronizada según lo elegido, y sin ejecutar ninguna acción sobre los
datos: cambia qué acciones quedan marcadas para \code{aplicar()}, no la
tabla. En una sesión no interactiva y sin \code{selector}, devuelve el plan
sin cambios.
}
\description{
Recorre los grupos pendientes y aquellos que contienen alternativas
destructivas. Muestra evidencia calculada sobre \code{datos}, las estrategias y
sus justificaciones, y devuelve el plan editado sin aplicarlo. En una sesión
no interactiva retorna inmediatamente el plan sin cambios, salvo que se
proporcione un \code{selector} explícito.
}
\details{
No se representa "no hacer nada" como una acción ficticia. Elegirlo cambia
\code{decision_grupo} a \code{"omitida"}; por contraste, un grupo aún no revisado
conserva \code{"pendiente"}. Cuando conservar los datos es la recomendación, esa
opción también se muestra con la marca "(Recomendado)" y su justificación.
Los diccionarios de capitalización se suministran como una lista con nombre
de vectores con nombre.
}
\examples{
datos <- data.frame(zona = c("Norte", "NORTE", "sur"))
plan <- planificar_limpieza(perfilar(datos), datos)
guiado <- guiar_limpieza(plan, datos)
identical(plan, guiado) # TRUE en una sesión no interactiva
}
\seealso{
\code{\link[=planificar_limpieza]{planificar_limpieza()}}, \code{\link[=aplicar]{aplicar()}}
}
