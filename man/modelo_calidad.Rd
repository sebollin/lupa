% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelo-calidad.R
\name{modelo_calidad}
\alias{modelo_calidad}
\alias{metrica}
\alias{especializar}
\alias{instanciar}
\alias{propiedades_metrica}
\alias{modelo}
\alias{metricas_nucleo}
\title{Construir métricas y modelos de calidad}
\usage{
metrica(
  nombre,
  semantica,
  granularidad,
  tipo_resultado,
  propiedades = character(),
  dimension = NA_character_,
  factor = NA_character_,
  metodo = NULL,
  validar_propiedades = NULL,
  orientacion = if (tipo_resultado \%in\% c("booleano", "real")) "conformidad" else
    "no_aplica"
)

especializar(metrica, nombre_especifico = NULL, ...)

instanciar(
  metrica_especifica,
  entidad,
  atributos = character(),
  nombre_instancia = NULL,
  metodo = NULL,
  referencial = NULL
)

propiedades_metrica(x)

modelo(..., marco = NULL)

metricas_nucleo()
}
\arguments{
\item{nombre}{Nombre estable y legible.}

\item{semantica}{Descripción de lo que mide la métrica.}

\item{granularidad}{Uno de los niveles de ontología devueltos por
\code{granularidades()} o su alias en la columna \code{relacional}. Los aliases se
normalizan al nombre de ontología antes de guardarse en la métrica.}

\item{tipo_resultado}{\code{"booleano"}, \code{"real"} en \verb{[0, 1]}, \code{"numero_real"},
\code{"entero"} no negativo o \code{"duracion"} no negativa. Los tres últimos
conservan resultados no acotados del catálogo y no admiten las cuatro
agregaciones normalizadas.}

\item{propiedades}{Nombres de las propiedades que fija \code{especializar()}.}

\item{dimension, factor}{Metadatos taxonómicos; no se usan para calcular
puntuaciones.}

\item{metodo}{Método predeterminado opcional. Es una función de \code{tablas} e
\code{instancia} que cumple el contrato descrito en \strong{Contrato de \code{metodo}}.}

\item{validar_propiedades}{Función opcional que recibe la lista con nombre
enviada a \code{\link[=especializar]{especializar()}} y debe devolver otra lista con nombre formada
sólo por propiedades declaradas. Puede validar alternativas, completar
valores predeterminados y normalizar la configuración. Si es \code{NULL}, todas
las propiedades declaradas son obligatorias y no se admiten otras.}

\item{orientacion}{Sentido de lectura del resultado: \code{"conformidad"} indica
que un valor mayor es mejor; \code{"defecto"}, que un valor menor es mejor; y
\code{"no_aplica"}, que la métrica no es una proporción interpretable en esos
términos. Es un vocabulario cerrado. Las métricas booleanas deben usar una
de las dos primeras y los resultados no acotados deben usar la última.}

\item{metrica}{Objeto de clase \code{metrica_generica}.}

\item{nombre_especifico}{Nombre de la especialización. Si se omite, conserva
el nombre genérico.}

\item{...}{En \code{especializar()}, propiedades con nombre de las declaradas en
\code{metrica(propiedades = )}; consúltelas con \code{\link[=propiedades_metrica]{propiedades_metrica()}}. En
\code{modelo()}, métricas instanciadas o una única lista que las contenga.}

\item{metrica_especifica}{Objeto de clase \code{metrica_especifica}.}

\item{entidad}{Nombres de las tablas ligadas, en el orden que espera el
método.}

\item{atributos}{Nombres de las columnas ligadas, en el mismo orden.}

\item{nombre_instancia}{Nombre de la instancia. Si se omite, se deriva de la
especialización y los objetos ligados.}

\item{referencial}{Objeto opcional creado por \code{\link[=referencial]{referencial()}}. Se conserva en
la instancia sin modificarlo: \code{instanciar()} no supone que toda métrica lo
use. El \code{metodo} debe leerlo y validar el contrato que necesite; las
métricas de \code{\link[=metricas_referencial]{metricas_referencial()}} hacen esa validación al medir.}

\item{x}{Métrica genérica, específica o instanciada.}

\item{marco}{Objeto opcional creado por \code{\link[=marco_calidad]{marco_calidad()}}. Cuando se provee,
todas las métricas instanciadas deben pertenecer a uno de sus pares
dimensión-factor.}
}
\value{
\code{metrica()} y \code{especializar()} devuelven closures S3;
\code{instanciar()} devuelve una \code{metrica_instanciada}; \code{modelo()} devuelve un
\code{modelo_calidad}; \code{metricas_nucleo()} devuelve una lista de métricas
genéricas. \code{propiedades_metrica()} devuelve un data frame con las
propiedades declaradas y si ya fueron configuradas.
}
\description{
\code{metrica()} declara una métrica genérica y devuelve una fábrica de closures.
\code{especializar()} fija sus propiedades de configuración; la closure específica
resultante puede instanciarse sobre distintas columnas o tablas.
\code{instanciar()} liga la métrica a objetos concretos y materializa el método de
medición. \code{modelo()} reúne métricas instanciadas sin calcular un índice global.
}
\details{
\code{metricas_nucleo()} devuelve veintidós métricas automatizables una vez
declaradas sus propiedades; \code{\link[=escala]{escala()}} y \code{\link[=vigencia]{vigencia()}} hacen explícitos los
insumos expertos que algunas necesitan. \code{\link[=metricas_referencial]{metricas_referencial()}} aporta por
separado las tres métricas que consumen un padrón tabular. Consulte
\code{\link[=catalogo_agesic]{catalogo_agesic()}} para la correspondencia completa.

En \code{ReglaIntegridadInterEntidad}, \code{entidad} y \code{atributos} se ligan como
\code{c(referencia, dependiente)} y \code{c(clave_primaria, clave_foranea)}.
Esta implementación calcula cobertura PK/FK como resultado real y sólo cubre
una parte de la genérica del marco, que declara granularidad
\code{conjuntoEntidades}, resultado booleano y admite además una expresión
condicional. \code{\link[=catalogo_agesic]{catalogo_agesic()}} deja visible esa cobertura parcial.
Pese a su nombre, \code{ErrorEstandar} sigue literalmente la semántica de la
tabla 16.5 del marco y devuelve la desviación estándar muestral sin
normalizar; exige al menos dos valores numéricos válidos. Por eso declara
\code{tipo_resultado = "numero_real"} y no admite \code{\link[=agregar]{agregar()}}.

\code{Formato} acepta exactamente una de las propiedades \code{expresion_regular},
\code{diccionario} o \code{validador}. Esta última permite conectar validadores
externos sin incorporarlos como dependencias.
Tanto \code{Formato} como \code{ValoresPosiblesPorExtension} omiten los valores \code{NA}:
un ausente no genera una medida en esas métricas y, por lo tanto, tampoco
integra el denominador de sus agregaciones. La completitud se mide por
separado con \code{NoNulo}; así un mismo ausente no se penaliza en dos factores.
\code{NoNulo} acepta el vector opcional \code{valores_nulos} para aplicar de forma
deliberada el diccionario de nulos que contempla el marco; sin configurarlo,
sólo considera los \code{NA} reales.
\code{ValoresPosiblesPorComprension} sigue la misma convención y acepta un
\code{predicado} o un rango definido por \code{minimo}, \code{maximo} e \code{inclusivo}.

Las métricas de duplicación marcan \strong{todas} las apariciones que participan
en un grupo repetido, no sólo la segunda y siguientes. \code{AtributoDuplicado}
omite ausentes. \code{ConjuntoAtributosDuplicado} compara las columnas ligadas y
\code{EntidadDuplicada} compara la fila completa cuando se instancia sin
atributos. Si se ligan atributos de clave, sigue la semántica del marco:
marca filas con la misma clave cuyos demás valores son iguales o ausentes en
alguna de las dos. En las comparaciones exactas, los \code{NA} forman parte de la
combinación comparada.

\code{EntidadContradictoria} compara los valores distintos de un atributo con la
misma normalización y medida de similitud que
\code{\link[=detectar_duplicados_aproximados]{detectar_duplicados_aproximados()}}. Marca las filas cuyo valor tiene un
vecino por debajo de \code{umbral}, pero no modifica datos ni propone una limpieza.
Su alcance se conserva en el atributo \code{alcance_metricas} del resultado de
\code{\link[=medir]{medir()}}: informa los valores distintos comparados, los pares evaluados y
los que quedaron bajo el umbral. \code{max_valores} permite acotar el vocabulario;
cuando se alcanza, el alcance declara el prefijo evaluado y los valores que
quedaron fuera.

\code{DesactualizacionPorFormato} devuelve \code{TRUE} cuando el valor \strong{no} cumple el
formato vigente. Conforme a las tablas 16.29 y 16.30 del marco,
\code{OportunidadAtributoPorFecha} indica si la fecha es anterior o igual a
\code{fecha_limite}, y \code{OportunidadAtributoPorIntervalo} si pertenece al intervalo
cerrado \verb{[inicio_vigencia, fin_vigencia]}; ambas son booleanas.

\code{GradoOportunidadAtributoPorFecha} y
\code{GradoOportunidadAtributoPorIntervalo} son extensiones propias basadas en el
curso CPAP, no entradas adicionales del catálogo AGESIC. Conservan la fórmula
continua \code{max(0, min(1, 1 - (t1 - t2) / (t3 - t2)))} para expresar cuánto
margen de utilidad queda. Exigen que \code{t3} sea posterior a \code{t2}; un intervalo
de duración cero se rechaza porque no define el cociente. Todas estas
métricas omiten los valores \code{NA}: su ausencia corresponde a completitud y no
genera una segunda medida de incumplimiento.

\strong{Desviación documentada del marco:} en \code{DensidadPonderada}, un atributo
más crítico recibe un coeficiente mayor y, si falta, produce una penalización
mayor. El texto del marco indica acercar a cero el coeficiente de mayor
gravedad, lo que penalizaría menos el ausente crítico y contradice el sentido
de la ponderación. Los coeficientes deben estar en \verb{[0, 1]} y sumar 1.

\code{tipo_resultado} es el contrato canónico que consultan las agregaciones. Las
unidades no forman parte de este núcleo porque el marco presenta ambas
nociones de forma inconsistente y sólo el tipo permite validar las fórmulas.
El argumento referencial se conserva sin transformación dentro de la
instancia; \code{\link[=metricas_referencial]{metricas_referencial()}} declara y valida el contrato específico
de correctitud semántica y cobertura.
}
\section{Contrato de \code{metodo}}{

El método tiene la firma \verb{function(tablas, instancia)}. \code{tablas} es una lista
con nombre de data frames, incluso cuando \code{\link[=medir]{medir()}} recibió una sola tabla.
\code{instancia} expone \code{entidad}, \code{atributos}, \code{configuracion}, \code{referencial} y
\code{declaracion}; el método decide cómo interpretar esos vínculos.

Debe devolver un data frame con exactamente una observación por objeto
medido y, como mínimo, estas columnas:
\itemize{
\item \code{resultado}: valor medido. Debe respetar \code{tipo_resultado}: lógicos sin \code{NA}
para \code{"booleano"}; números finitos en \verb{[0, 1]} para \code{"real"}; números
finitos para \code{"numero_real"}; enteros o duraciones no negativos para los
tipos homónimos;
\item \code{entidad}: nombre de la tabla a la que corresponde la medida;
\item \code{atributo}: nombre de la columna o \code{NA_character_} cuando no corresponde;
\item \code{fila}: posición de la fila o \code{NA_integer_} para resultados agregados;
\item \code{objeto}: etiqueta legible y estable del objeto medido.
}

Las columnas adicionales se descartan. Un \code{metodo} pasado a \code{\link[=instanciar]{instanciar()}}
reemplaza el predeterminado sólo para esa instancia. El ejemplo ejecutable
muestra la cadena genérica → específica → instanciada completa.
}

\section{Contrato de propiedades}{

\code{propiedades} declara los nombres que puede recibir \code{\link[=especializar]{especializar()}}. Sin
\code{validar_propiedades}, todas son obligatorias. Con un validador propio, éste
recibe la lista \code{configuracion}, debe rechazar combinaciones inválidas y puede
devolver sólo el subconjunto activo o añadir valores predeterminados, pero no
propiedades ajenas a la declaración. \code{\link[=propiedades_metrica]{propiedades_metrica()}} permite consultar
los nombres aceptados sin inspeccionar atributos internos de las closures.
}

\examples{
nucleo <- metricas_nucleo()
no_nulo <- especializar(
  nucleo$NoNulo, nombre_especifico = "NoNuloEdad"
)
instancia <- instanciar(no_nulo, entidad = "personas", atributos = "edad")
modelo_calidad <- modelo(instancia)
medir(modelo_calidad, data.frame(edad = c(20, NA, 35)))

# Las fábricas también se pueden encadenar: genérica() -> específica().
instancia_directa <- nucleo$NoNulo()(
  entidad = "personas", atributos = "edad"
)
propiedades_metrica(nucleo$Formato)

# Métrica propia: las dos llamadas encadenadas son especializar e instanciar.
metodo_origen <- function(tablas, instancia) {
  x <- tablas[[instancia$entidad]][[instancia$atributos]]
  filas <- seq_along(x)
  data.frame(
    resultado = !is.na(x) & nzchar(x),
    entidad = instancia$entidad,
    atributo = instancia$atributos,
    fila = filas,
    objeto = paste0(instancia$entidad, "$", instancia$atributos,
                    "[", filas, "]")
  )
}
OrigenDeclarado <- metrica(
  "OrigenDeclarado", "Indica si se declaró el origen del registro.",
  "instanciaAtributo", "booleano", orientacion = "conformidad",
  dimension = "Trazabilidad", factor = "Origen documentado",
  metodo = metodo_origen
)
origen <- OrigenDeclarado()(
  entidad = "entrega", atributos = "origen"
)
medir(
  modelo(origen),
  data.frame(origen = c("sistema_a", "", NA), stringsAsFactors = FALSE)
)

# Especialización oficial de teléfono fijo según el formato vigente del PNN.
telefono_pnn <- especializar(
  nucleo$DesactualizacionPorFormato,
  nombre_especifico = "TelefonoFijoPNN",
  expresion_regular = "^[0-9]{8}$"
)

# La métrica oficial es booleana y recibe la fecha límite Tf.
a_tiempo <- especializar(
  nucleo$OportunidadAtributoPorFecha,
  nombre_especifico = "EntregaATiempo",
  fecha_limite = as.Date("2026-06-30")
)
medir(
  modelo(instanciar(a_tiempo, "entregas", "fecha")),
  data.frame(fecha = as.Date(c("2026-06-29", "2026-07-01")))
)

# La extensión continua conserva cuánto margen de utilidad queda.
grado <- especializar(
  nucleo$GradoOportunidadAtributoPorFecha,
  fecha_solicitud = as.Date("2026-06-01"),
  fecha_fin_utilidad = as.Date("2026-07-01")
)

# Formato(NumeroDocumento, DNIC) se obtiene conectando el validador incluido:
cedula_dnic <- especializar(
  nucleo$Formato, nombre_especifico = "NumeroDocumentoDNIC",
  validador = validar_ci_uy
)

}
\references{
\href{https://www.gub.uy/agencia-gobierno-electronico-sociedad-informacion-conocimiento/}{AGESIC (2020)}.
\emph{Marco de trabajo para la Gestión de la Calidad
de Datos en Gobierno Digital}, versión 1.6, Presidencia de la República,
Uruguay.

Curso CPAP, material \emph{Evaluación de Calidad}: fórmula continua de
oportunidad implementada bajo los nombres \verb{GradoOportunidadAtributo*}.
}
\seealso{
\code{\link[=referencial]{referencial()}}, \code{\link[=agregar]{agregar()}}, \code{\link[=medir]{medir()}}, \code{\link[=proponer_modelo]{proponer_modelo()}}
}
