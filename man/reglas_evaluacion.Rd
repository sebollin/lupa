% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluacion.R
\name{reglas_evaluacion}
\alias{reglas_evaluacion}
\alias{regla_evaluacion}
\alias{perfil_evaluacion}
\alias{perfiles_madurez}
\title{Reglas y perfiles de evaluación}
\usage{
regla_evaluacion(
  nombre,
  condicion,
  metricas = NULL,
  proporcion_minima = NULL,
  desenlace = NULL
)

perfil_evaluacion(nombre, ...)

perfiles_madurez(metricas = NULL, umbrales = NULL)
}
\arguments{
\item{nombre}{Nombre de la regla o del perfil.}

\item{condicion}{Función de un argumento que recibe el vector \code{resultado} de
las medidas seleccionadas, en el orden de la tabla, y debe devolver un
vector lógico sin ausentes de la misma longitud. No modifica las medidas.}

\item{metricas}{Nombres de métricas instanciadas a las que se aplica la
regla, es decir, valores de la columna \code{metrica_instanciada}. \code{NULL}, el
valor predeterminado, aplica la condición a todas.}

\item{proporcion_minima}{\code{NULL}, para conservar una regla por medida, o un
número entre \code{0} y \code{1} que declara la proporción mínima de medidas que
deben cumplir \code{condicion}. En este segundo caso la regla es agregada: el
umbral queda guardado en el objeto y \code{\link[=evaluar]{evaluar()}} publica la proporción, el
veredicto y el universo de medidas que la produjo.}

\item{desenlace}{\code{NULL}, para limitar la regla a evaluar, o \code{"suprimir"}
para declarar que las medidas que no cumplen \code{condicion} no deben
publicarse. No existe un desenlace predeterminado.}

\item{...}{Reglas creadas por \code{regla_evaluacion()} o una única lista que las
contenga.}

\item{umbrales}{Vector numérico con nombres, estrictamente creciente y en
\verb{[0, 1]}. \code{NULL} conserva los tres perfiles incluidos de fábrica.}
}
\value{
\code{regla_evaluacion()} devuelve una \code{regla_evaluacion};
\code{perfil_evaluacion()} devuelve un \code{perfil_evaluacion}; y
\code{perfiles_madurez()} devuelve una lista de perfiles.
}
\description{
Una regla aplica una condición a los resultados de una o más métricas
instanciadas. Un perfil reúne reglas y su evaluación es la media aritmética
simple de las evaluaciones de esas reglas; no es un índice de dimensión ni
un índice global de calidad.
}
\details{
\code{perfiles_madurez()} crea por omisión los perfiles \code{Básico}, \code{Intermedio} y
\code{Avanzado} de AGESIC, con condiciones estrictas \verb{> 0.5}, \verb{> 0.7} y \verb{> 0.9}.
El argumento \code{umbrales} permite construir otra familia con nombres y cortes
crecientes propios sobre las mismas métricas instanciadas.

\code{regla_evaluacion()} almacena la función sin ejecutarla. \code{\link[=evaluar]{evaluar()}}
selecciona las medidas mediante \code{metricas}, llama una vez a \code{condicion} y
rechaza resultados que no sean lógicos, que tengan otra longitud o que
contengan \code{NA}. Si \code{proporcion_minima} no es \code{NULL}, calcula sobre esos
mismos lógicos la proporción que cumple y la compara mediante \code{>=} con el
umbral declarado; no pondera medidas ni construye un puntaje global. Las
evaluaciones cuyas reglas no declaran \code{desenlace} conservan su estructura
anterior. Cuando una regla declara \code{desenlace = "suprimir"}, \code{\link[=evaluar]{evaluar()}}
añade un plan trazable con una fila por medida incumplida y por regla; no
modifica la medición ni los datos que la originaron. La función expresa un
criterio de evaluación; no es un método de medición ni recibe el data frame
original. Si ningún nombre de
\code{metricas} coincide, el error enumera tanto los nombres solicitados como
las métricas instanciadas disponibles, que normalmente tienen la forma
\code{MetricaEspecifica@entidad.atributo}.
}
\examples{
regla <- regla_evaluacion("Completitud suficiente", function(x) x > 0.9)
regla_70 <- regla_evaluacion(
  "Al menos 70 \%", function(x) x > 0.9, proporcion_minima = 0.7
)
regla_publicacion <- regla_evaluacion(
  "Medida publicable", function(x) x > 0.9, desenlace = "suprimir"
)
perfil <- perfil_evaluacion("Operativo", regla)
madurez <- perfiles_madurez("NoNulo")
propios <- perfiles_madurez(
  "NoNulo", c(Exploratorio = 0.3, Operativo = 0.65, Consolidado = 0.85)
)
names(madurez)
names(propios)
perfil$nombre
}
\seealso{
\code{\link[=medir]{medir()}}, \code{\link[=evaluar]{evaluar()}}, \code{\link[=perfiles_madurez]{perfiles_madurez()}}

\code{\link[=regla_evaluacion]{regla_evaluacion()}}, \code{\link[=comparar_evaluaciones]{comparar_evaluaciones()}},
\code{\link[=historico_calidad]{historico_calidad()}}

\code{\link[=evaluar]{evaluar()}}, \code{\link[=detectar_deriva_calidad]{detectar_deriva_calidad()}}
}
