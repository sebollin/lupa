% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profiling-avanzado.R
\name{detectar_asociaciones}
\alias{detectar_asociaciones}
\title{Detectar asociaciones entre columnas}
\usage{
detectar_asociaciones(
  datos,
  dependencias = NULL,
  umbral = 0.3,
  muestra = 10000,
  max_columnas = 50L,
  max_niveles = 50L,
  max_pares = 500L
)
}
\arguments{
\item{datos}{Tabla que se desea examinar.}

\item{dependencias}{Resultado opcional de \code{\link[=detectar_dependencias]{detectar_dependencias()}}.}

\item{umbral}{Valor mínimo en \verb{[0, 1]} que se informa.}

\item{muestra}{Máximo común de filas; \code{Inf} desactiva el muestreo.}

\item{max_columnas}{Máximo de columnas analizables.}

\item{max_niveles}{Máximo de niveles para tratar una columna como categórica.}

\item{max_pares}{Máximo de asociaciones devueltas después de ordenar.}
}
\value{
Data frame S3 \code{asociaciones_columnas}. Sus atributos declaran filas,
columnas y pares examinados, omisiones por dependencia y truncamiento.
}
\description{
Calcula Pearson entre numéricas, V de Cramér entre categóricas y eta cuadrado
entre una categórica y una numérica. Las tres medidas se informan en \verb{[0, 1]}:
Pearson usa su valor absoluto. La tabla declara el método, soporte y posible
muestreo; no presenta significancia estadística.
}
\details{
Se descartan constantes, fechas, listas, categóricas de cardinalidad alta y
columnas posteriores a \code{max_columnas} antes de construir pares. Las
dependencias funcionales exactas recibidas en \code{dependencias} no se repiten
como asociaciones.
}
\examples{
d <- data.frame(x = 1:20, y = 2 * (1:20), grupo = rep(c("A", "B"), 10))
detectar_asociaciones(d, umbral = 0)
}
\seealso{
\code{\link[=detectar_dependencias]{detectar_dependencias()}}, \code{\link[=analizar]{analizar()}}
}
