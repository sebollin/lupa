% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perfilar.R
\name{perfilar}
\alias{perfilar}
\title{Perfilar un conjunto de datos}
\usage{
perfilar(
  datos,
  nombre = .nombre_de_los_datos(substitute(datos)),
  fecha = Sys.time(),
  muestra = 1e+05,
  max_patrones = 20,
  distinguir_mayusculas = TRUE,
  expandir = FALSE,
  umbral_alta_cardinalidad = 0.5,
  umbral_faltantes_sospechoso = 0.1,
  clave = NULL,
  umbral_faltantes_error = 0.4,
  umbral_patron_raro = 0.05,
  umbral_patron_dominante = 0.5,
  columnas_sin_ceros = character(),
  columnas_no_negativas = character(),
  columnas_opcionales = character(),
  aplicabilidad = NULL,
  ausencia_estructural = TRUE,
  sentinelas_numericos = c(-9, -99, -999, -9999, 999),
  analizar_dependencias = TRUE,
  umbral_dependencia = 0.995,
  umbral_casi_clave_dependencia = 0.8,
  max_columnas_dependencias = 100L,
  datos_personales_permitidos = TRUE,
  proteger_datos_personales = TRUE,
  columnas_personales = character(),
  validadores_personales = NULL,
  umbral_documento_verificado = 0.9,
  muestra_validadores = 1000L,
  duplicados_aproximados = FALSE,
  normalizar = TRUE,
  max_filas_hallazgo = 1000L,
  umbral_orden_columnas = 0.95,
  max_columnas_orden = 20L,
  umbral_solapamiento_orden = 0.1,
  umbral_aritmetica = 0.9,
  min_filas_aritmetica = 3L,
  tolerancia_aritmetica = 1e-08,
  max_columnas_aritmetica = 20L,
  casi_duplicados_vocabulario = TRUE,
  max_proporcion_grupo_vocabulario = 0.5,
  umbral_variante_rara_vocabulario = 0.05,
  min_asimetria_vocabulario_corto = 10,
  min_asimetria_vocabulario = 2,
  min_participacion_dominante_vocabulario_corto = 0.5,
  variantes_equifrecuentes_vocabulario = FALSE,
  max_asimetria_equifrecuente_vocabulario = 2,
  max_comparaciones_dependencias = 200000L,
  max_trabajo_vocabulario = 2e+10,
  max_trabajo_dependencias = 1e+08,
  max_largo_valor_vocabulario = .MAX_LARGO_VALOR_CASI_DUPLICADOS,
  avisar_costo_tabla_ancha = TRUE,
  umbral_celdas_aviso_tabla_ancha = .UMBRAL_CELDAS_AVISO_TABLA_ANCHA
)
}
\arguments{
\item{datos}{Objeto que hereda de \code{data.frame} —\code{tibble} y \code{data.table}
entran por ahí— o una matriz de dos dimensiones, que se convierte con
\code{\link[=as.data.frame]{as.data.frame()}}. La conversión queda declarada en \code{meta$entrada_convertida}
para que el perfil no aparente haber recibido lo que no recibió.}

\item{nombre}{Nombre descriptivo del objeto.}

\item{fecha}{Fecha y hora de la corrida. Se puede fijar para construir series
reproducibles; se normaliza a UTC.}

\item{muestra}{Máximo de filas usadas para patrones e inferencia de tipos.
Use \code{Inf} para analizar todas las filas.}

\item{max_patrones}{Máximo de patrones mostrados por columna.}

\item{distinguir_mayusculas}{Si se distinguen mayúsculas y minúsculas.}

\item{expandir}{Si se emite un token por carácter en los patrones.}

\item{umbral_alta_cardinalidad}{Umbral sobre la tasa de valores distintos
de una columna categórica. No alcanza por sí solo: el hallazgo exige
además al menos diez valores distintos, porque con pocos la tasa está
dominada por el tamaño de la tabla —dos valores en tres filas dan 0,67 y
superan cualquier umbral razonable— y una columna de dos valores no
puede tener cardinalidad alta.}

\item{umbral_faltantes_sospechoso}{Umbral inferior de faltantes. El
hallazgo se activa al superarlo en sentido estricto.}

\item{clave}{Nombres de las columnas que identifican una fila. Cuando se
declaran, la trazabilidad de cada hallazgo trae además el valor de esas
columnas para las filas señaladas, de modo que el caso se pueda verificar
en el sistema de origen sin abrir la tabla. El índice de fila se conserva
siempre. La clave declarada se trata como sensible: si la protección de
datos personales está activa y alguna de esas columnas se clasifica como
personal, sus valores salen enmascarados igual que la evidencia.
La comprobación de una clave tiene dos ejes independientes:
\code{meta$clave$unicidad} comprueba si sus combinaciones son únicas con la
semántica de R,
y \code{meta$clave$ausencia_nulos} comprueba que sus componentes no tengan
valores ausentes. Cada eje declara \code{verificada}, \code{refutada} o
\code{no_verificada}; una clave con ambos ejes verificados conserva exactamente
el objeto histórico y no agrega metadatos. Cuando un eje falla o no se
puede comprobar, \code{meta$clave$trazabilidad} explica que la localización
agrupa con la semántica de R, incluso si el motor SQL trata dos \code{NULL} como
distintos. Por eso una clave puede tener a la vez una colisión para la
trazabilidad y una ausencia que impide la garantía \verb{NOT NULL}.}

\item{umbral_faltantes_error}{Umbral por encima del cual los faltantes son
un error; la igualdad conserva la severidad sospechosa.}

\item{umbral_patron_raro}{Máxima frecuencia de un patrón raro.}

\item{umbral_patron_dominante}{Frecuencia mínima del patrón dominante.}

\item{columnas_sin_ceros}{Nombres de columnas donde cero no es admisible.}

\item{columnas_no_negativas}{Nombres de columnas que deben ser no negativas.}

\item{columnas_opcionales}{Nombres de columnas donde la ausencia no es un
defecto. Su universo de completitud son las celdas presentes, y
\code{cobertura_diagnosticos} declara el recorte. Sirve para el vacío por
diseño: un historial con vigencia abierta, una columna que sólo
corresponde a algunas filas.}

\item{aplicabilidad}{Lista con nombre por columna, donde cada elemento es
una fórmula de un solo lado evaluada sobre \code{datos} — por ejemplo
\code{list(marca_auto = ~ tiene_auto == "Si")}. Las filas donde el predicado no
se cumple salen del universo de esa columna: no cuentan como ausencia. Las
filas donde el predicado no se puede determinar se declaran aparte, sin
contarse ni como aplicables ni como no aplicables. Un valor presente fuera
del universo produce el hallazgo \code{valor_fuera_de_aplicabilidad}, que es el
error simétrico y hoy no tiene otra forma de aparecer.

\code{lupa} no infiere el universo: si nadie lo declara, toda la columna aplica
y el resultado es el de siempre. Declararlo es lo que distingue el vacío
por diseño del vacío por error, y sin esa distinción una tabla sana puede
informar completitud baja siendo completa.

\strong{Hasta dónde llega el universo.} Gobierna todo el perfilado: los conteos,
las proporciones, los hallazgos y las acciones que propone
\code{\link[=planificar_limpieza]{planificar_limpieza()}}. Medido sobre una columna condicionada de mil filas
con universo de trescientas, treinta de ellas vacías: declarándolo, la
proporción de faltantes es \verb{0,100}, no hay hallazgo y el plan no propone
nada; sin declararlo son \verb{0,730}, sale \code{faltantes} y el plan propone dos
acciones.

La medición contra un marco recibe la misma declaración: \code{\link[=medir]{medir()}} acepta
\code{aplicabilidad} y recorta las filas antes de medir, así que el histórico y
la deriva —que consumen mediciones, no perfiles— heredan el número
correcto. El \code{aplicabilidad} de \code{\link[=marco_calidad]{marco_calidad()}} es otra cosa: dice si un
factor aplica a datos temporales o geométricos, no qué filas entran al
universo.}

\item{ausencia_estructural}{Si se busca evidencia de que la ausencia de una
columna es por diseño. \code{lupa} no infiere el universo ni lo cambia por su
cuenta, pero declarar \code{aplicabilidad} exige saber que existe: quien perfila
una tabla con columnas condicionadas sin declarar nada recibe el mismo
informe engañoso que si el vacío fuera un defecto. Con \code{TRUE}, cuando el
valor de otra columna decide qué filas tienen ésta, o cuando dos columnas
se reparten las filas sin pisarse, se emite \code{posible_ausencia_estructural}
con severidad \code{ok}, la evidencia medida y la línea exacta que habría que
escribir para declararlo. Sugiere; no decide. Las columnas ya declaradas
quedan fuera del examen.

Con el mismo argumento viaja \code{regla_silencia_ausencia}, también \code{ok}: avisa
cuando una columna declarada opcional o con universo propio sigue casi
vacía dentro de ese universo. La declaración funcionó y por eso el perfil
salió limpio; el aviso existe para que eso sea una decisión y no un efecto.}

\item{sentinelas_numericos}{Vector completo de valores numéricos que se
interpretan como ausencia. \code{numeric()} los desactiva; las cadenas de
ausencia se siguen evaluando por separado.}

\item{analizar_dependencias}{Si se buscan dependencias funcionales entre
pares de columnas. Se aplica una sola muestra común a toda la tabla.}

\item{umbral_dependencia}{Cumplimiento mínimo para informar una dependencia.}

\item{umbral_casi_clave_dependencia}{Tasa de valores distintos a partir de
la cual un determinante se descarta como casi-clave antes de agrupar.}

\item{max_columnas_dependencias}{Máximo de columnas que intervienen en la
búsqueda, cuyo costo crece cuadráticamente.}

\item{datos_personales_permitidos}{Si la entrega admite datos personales.
El valor predeterminado no juzga su presencia: la clasificación se informa
con severidad \code{"ok"}. Use \code{FALSE} sólo cuando el contrato de la entrega
declare que no deben existir.}

\item{proteger_datos_personales}{Si se reemplazan modas, ejemplos, evidencia
y estadísticos de orden concretos cuando \code{poder_discriminante} es medio,
alto o verificado. Las clasificaciones débiles se conservan como aviso pero
no suprimen estadísticos. Para conservar todo en el objeto debe desactivarse
explícitamente; \code{\link[=reportar]{reportar()}} aplica además su propia protección
predeterminada.}

\item{columnas_personales}{Columnas que traen datos personales, declaradas
por quien conoce el dato. \code{c("cod_benef", "apodo")} las nombra sin decir de
qué tipo son; \code{c(cod_benef = "documento_identidad")} además lo dice. Lo
declarado gana sobre lo inferido y no se vuelve a examinar.

Existe porque el léxico de nombres de columna \strong{no puede ser completo}:
una columna con documentos se puede llamar de cualquier manera, y ninguna
lista de nombres frecuentes la va a reconocer. Es el mismo patrón que
\code{columnas_opcionales}, aplicado a la otra decisión que el paquete no puede
tomar solo.}

\item{validadores_personales}{Pack o lista nombrada de funciones que reciben
un vector de texto y devuelven un lógico de igual longitud. \code{NULL} usa
\code{validadores_uruguay()} por compatibilidad; \code{FALSE} o \code{numeric()} desactiva
la verificación de documentos. El nombre del mejor validador queda en el
fundamento de la clasificación.}

\item{umbral_documento_verificado}{Proporción mínima de valores que debe
aceptar un validador para clasificar una forma de documento como
\code{verificado}. Por defecto es \code{0.9}.}

\item{muestra_validadores}{Máximo de valores usados en el filtro preliminar
de cada validador. Si la proporción preliminar ya queda bajo el umbral no
se valida la columna completa; use \code{Inf} para revisar todos desde el inicio.}

\item{duplicados_aproximados}{\code{FALSE} por omisión. Use \code{TRUE} o una lista de
argumentos para ejecutar \code{\link[=detectar_duplicados_aproximados]{detectar_duplicados_aproximados()}} y añadir sus
pares y hallazgos al perfil. Es un análisis acotado y opcional porque no
afirma identidad ni debe encarecer todas las corridas.}

\item{normalizar}{Perfil de comparación que se conserva en \code{meta$normalizacion}
y que heredan los análisis de duplicados y claves cuando no reciben uno
explícito. Cambia sólo la representación usada para comparar.}

\item{max_filas_hallazgo}{Tope de índices de fila que conserva cada
trazabilidad disponible. Por defecto es \code{1000}; cuando se supera, el
estado queda como \code{truncada} y el total se conserva. Use \code{Inf} sólo si
necesita desactivar explícitamente el tope.}

\item{umbral_orden_columnas}{Cumplimiento mínimo de una relación de orden
entre columnas comparables. Se usa \code{0.95} por omisión; con menos de 20
filas comparables se permite una sola inversión para no descartar tablas
pequeñas. El alcance efectivo queda en \code{meta$orden_columnas}.}

\item{max_columnas_orden}{Máximo de columnas numéricas o temporales que se
comparan entre sí para detectar relaciones de orden. Las columnas que
exceden el límite se conservan en \code{meta$orden_columnas$columnas_omitidas}.}

\item{umbral_solapamiento_orden}{Solapamiento mínimo de los rangos
intercuartiles para considerar que dos columnas representan magnitudes
comparables. Por defecto es \code{0.1}: al menos una décima parte del rango
intercuartílico más ancho debe ser común a ambos. Esto evita interpretar
como restricción fila a fila un orden explicado sólo por escalas
separadas. Si no hay ese solapamiento, una brecha con IQR exactamente cero
conserva el par porque la mitad central sostiene el mismo desplazamiento
fila a fila. No se aplica una tolerancia oculta. Use \code{0} para desactivar el
filtro de magnitud. Ambos criterios se publican en la evidencia. Los pares
descartados se cuentan en
\code{meta$orden_columnas$pares_descartados_magnitud} y los recuperados en
\code{meta$orden_columnas$pares_rescatados_brecha_estable}.}

\item{umbral_aritmetica}{Proporción mínima de filas comparables que deben
satisfacer una identidad dentro de \code{tolerancia_aritmetica} para reconocer
una regularidad aritmética entre columnas numéricas. El valor por omisión
es \code{0.9}. Una vez reconocida la relación se informan todas sus
discrepancias, sin un segundo filtro por su cantidad absoluta. La
proporción y el criterio efectivos se publican en cada evidencia.}

\item{min_filas_aritmetica}{Mínimo de filas comparables necesario para
evaluar una candidata aritmética. Por omisión es \code{3}.}

\item{tolerancia_aritmetica}{Tolerancia numérica relativa escalada usada al
comparar un valor observado y uno esperado. Por omisión es \code{1e-8}; el
criterio completo y el valor efectivo se declaran en cada evidencia.}

\item{max_columnas_aritmetica}{Máximo de columnas numéricas que intervienen
en la búsqueda aritmética, cuyo costo crece cúbicamente. Por omisión es
\code{20}. Si se omiten columnas, \code{cobertura_diagnosticos} declara el recorte y
\code{meta$aritmetica_columnas} conserva los conteos de combinaciones.}

\item{casi_duplicados_vocabulario}{Lógico que activa el diagnóstico de
variantes casi duplicadas dentro del vocabulario de cada columna de texto.
Por defecto es \code{TRUE}; \code{FALSE} lo omite sin afectar los demás hallazgos.
La distancia es una señal heurística, no una prueba de identidad: Jaro--Winkler
puede agrupar nombres de calles o códigos que sólo comparten un prefijo o un
sufijo. En vocabularios heterogéneos revise la evidencia como sospechosa,
declare una regla de dominio o use \code{FALSE} para desactivar este diagnóstico.}

\item{max_proporcion_grupo_vocabulario}{Proporción máxima del vocabulario
que puede abarcar el grupo mayor para entregar grupos de variantes. Por
defecto es \code{0.5}; si se supera, el alcance declara que el diagnóstico no
aplica en vez de entregar un bloque que abarque casi toda la columna.}

\item{umbral_variante_rara_vocabulario}{Proporcion maxima de la columna que
puede ocupar una variante breve para abrir la comparacion por una edicion.}

\item{min_asimetria_vocabulario_corto}{Razon minima entre la frecuencia de
una forma dominante y una variante breve para abrir la comparacion por una
edicion.}

\item{min_asimetria_vocabulario}{Razón mínima entre la frecuencia de la
forma dominante y la de la variante para abrir un grupo por la vía general
de distancia. Por omisión \code{2}. Una asimetría de \verb{1,5} significa que las dos
formas son casi igual de comunes, que es evidencia muy floja de una errata:
medido sobre tablas limpias y sobre erratas sembradas, los falsos positivos
están entre \verb{1,0} y \verb{1,5} y las erratas reales desde \verb{9,0}.}

\item{min_participacion_dominante_vocabulario_corto}{Proporcion minima de la
columna que debe ocupar la forma dominante en la comparacion por una
edicion.}

\item{variantes_equifrecuentes_vocabulario}{Si se informa el diagnóstico
\code{variantes_equifrecuentes_vocabulario}, que señala dos formas cercanas que
se reparten la columna sin que ninguna sea dominante —el error sistemático
de dos operadores, una plantilla rota o una migración parcial—.

\strong{Está apagado por omisión y la razón está medida}: sobre la batería de
31 tablas limpias produce un grupo sospechoso donde no hay defecto. Lo que
lo dispara no es el tamaño de la tabla sino el reparto: dos formas
parecidas con frecuencias del mismo orden lo activan igual con cuatro filas
que con quinientas, y en una tabla chica ese reparto sale por casualidad
más seguido. Decía «tablas de menos de veinte filas», y se midió de cuatro
a quinientas: dispara en todas. Es aditivo: encenderlo no cambia ni pierde ninguna
detección de \code{casi_duplicados_vocabulario}. El límite que deja de cubrir se
declara igual, encendido o apagado, en \code{n_grupos_sin_variante_rara}.}

\item{max_asimetria_equifrecuente_vocabulario}{Razón máxima entre la
frecuencia mayor y la menor para considerar que dos formas se reparten la
columna. Con \code{2}, \code{40} contra \code{5} no entra y \code{5} contra \code{5} sí.}

\item{max_comparaciones_dependencias}{Tope de pares determinante-dependiente
que se comparan. El costo de las dependencias es del orden de
\verb{columnas^2 x filas} y empeora con determinantes casi únicos; cuando el
presupuesto se agota, lo comparado se informa y lo que quedó sin comparar
se declara en \code{cobertura_diagnosticos}, nunca como cero.}

\item{max_trabajo_vocabulario}{Tope de comparaciones de carácter para las
comparaciones de distancia del vocabulario. Comparar dos valores cuesta del
orden del producto de sus largos, así que el trabajo es la suma de ese
producto sobre todos los pares. Contar pares por longitud media subestima
las cadenas largas: medido, el mismo presupuesto compraba 5,3 millones de
unidades por segundo con valores de 900 caracteres y 44 millones con
valores de 40. Se combina con \code{max_pares}, manda el más restrictivo, y el
recorte declara valores, pares y trabajo sin comparar. \code{Inf} lo desactiva.}

\item{max_trabajo_dependencias}{Tope predeterminado de unidades fila-par para
las dependencias. Se combina con \code{max_comparaciones_dependencias}; el
límite efectivo baja cuando hay muchas filas. \code{Inf} desactiva este tope,
pero no el de pares.}

\item{max_largo_valor_vocabulario}{Maximo de caracteres permitido en cada
valor que entra en la comparacion de casi duplicados del vocabulario. Por
defecto es \code{10000}, elegido porque la distancia normalizada deja de
distinguir de forma estable una diferencia local de muchas diferencias en
textos largos. Una columna que supera el tope se declara completa fuera
de alcance en \code{cobertura_diagnosticos}; no se recortan valores en silencio.
\code{Inf} recupera explicitamente el comportamiento anterior sin tope.}

\item{avisar_costo_tabla_ancha}{Si es \code{TRUE}, avisa en sesiones interactivas
cuando las celdas proyectadas superan \code{umbral_celdas_aviso_tabla_ancha}.
El aviso es una estimacion y queda en silencio en scripts no interactivos.}

\item{umbral_celdas_aviso_tabla_ancha}{Cantidad de celdas a partir de la cual
se emite el aviso de tabla ancha. Por defecto, \code{100000}; \code{Inf} lo desactiva
explicitamente.}
}
\value{
Objeto S3 de clase \code{perfil}. Cada fila de hallazgos incluye
n_evaluados, n_afectados y unidad_conteo: son conteos de las unidades
declaradas (por ejemplo fila, columna, formato o par). En
\code{mayusculas_inconsistentes}, \code{normalizacion_unicode} y
\code{casi_duplicados_vocabulario}, la unidad es
\code{valor_distinto}: \code{n_evaluados} cuenta los valores distintos evaluados y
\code{n_afectados} los valores distintos que participan en la colisión. Su
\code{trazabilidad} sigue siendo por fila y enumera todas las filas que
contienen esos valores, no sólo las filas defectuosas; por eso su total
puede ser mayor que \code{n_afectados}. En \code{casi_duplicados_vocabulario}, la
traza incluye todas las filas cuyos
valores pertenecen al grupo elegido, incluida la forma dominante. La
distancia es una senal heuristica, no una prueba de que cada fila deba
corregirse; la evidencia declara cuantas filas mostradas pertenecen a las
formas variantes y cuantas a las formas dominantes.
En \code{filas_duplicadas}, el conteo y la traza incluyen todas las filas
participantes de los grupos; el numero de excedentes queda en la
evidencia. Cuando el camino
no puede conocer un conteo, informa NA, nunca cero. La columna de lista
\code{trazabilidad} distingue \code{disponible}, \code{truncada}, \code{no_aplica} y
\code{no_disponible}; cuando corresponde conserva índices de fila acotados por
\code{max_filas_hallazgo}, el total conocido y el alcance. En
\code{casi_duplicados_vocabulario}, donde la traza mezcla filas de formas
variantes con filas de la forma dominante, conserva además
\code{n_filas_formas_variantes} y \code{n_filas_formas_dominantes} con el reparto
completo, y \code{mostrados_formas_variantes} y \code{mostrados_formas_dominantes}
con el reparto de lo que sobrevivió al truncado. Las variantes se entregan
primero, de modo que el truncado no se lleve lo accionable. Para
\code{patron_raro},
el alcance puede ser \code{completo}, \code{muestra_patrones},
\code{patrones_parciales} o \code{muestra_patrones+patrones_parciales}. El resumen y
el texto de evidencia de \code{patron_raro} muestran como maximo seis patrones,
pero la trazabilidad conserva los nombres de todos los patrones raros hasta
un limite de 5.000; \code{patrones_parciales} indica que se alcanzo ese limite,
no que se haya alcanzado el tope de presentacion.
Cuando se emite un hallazgo \code{patron_raro}, su evidencia incluye la
proporcion del patron dominante y cuantas filas pertenecen a patrones no
dominantes que superan \code{umbral_patron_raro} y por eso quedan excluidos.
Si el patron dominante no alcanza \code{umbral_patron_dominante}, no se emite
el hallazgo: \code{cobertura_diagnosticos} declara la no medicion, su proporcion
observada y el argumento que se puede ajustar.
Si el conteo y la traza no coinciden, conserva el hallazgo y emite una
advertencia de clase \code{lupa_trazabilidad_incoherente}. La guarda compara el
total previo al truncado y respeta la unidad declarada.
Una matriz no analizada conserva en la traza todas sus filas. Si una
columna de listas se reconoce como constante pero no se puede contar su
frecuencia, el conteo afectado queda en NA y \code{cobertura_diagnosticos}
explica la no evaluación.
Los índices no contienen valores. Usarlos para extraer filas de los datos
originales puede volver a exponer datos personales; el paquete no realiza
esa extracción y la protección de salidas no sustituye el control de acceso
a los datos de entrada.
En la evidencia de \code{casi_duplicados_vocabulario}, \code{clase_diferencia} puede
ser \code{normalizacion_exacta} (la coincidencia aparece después de normalizar),
\code{dentro_de_palabra} (la diferencia está dentro de un token),
\code{token_completo} (cambian tokens completos), \code{token_unico} (ambos valores
son un solo token y esa distinción estructural no aplica), \code{mixta} (el grupo
reúne aristas de más de una clase) o \code{indeterminada} (no hubo aristas
clasificables). Son categorías de evidencia, no veredictos de identidad.
\code{cobertura_diagnosticos} es una tabla hermana de \code{hallazgos}, con una fila
por diagnóstico que no pudo evaluarse o cuya enumeración quedó parcial y
las columnas \code{diagnostico},
\code{columna}, \code{motivo}, \code{como_resolverlo} y \code{dependencia}. Incluye la falta de
\code{stringdist}, \code{stringi}, \code{bit64} o \code{sf}, y las zonas horarias POSIXt sin
declarar. Los patrones de frecuencia intermedia no se consideran desvios
del patron dominante: \code{patron_raro} es completo respecto de su criterio de
rareza cuando no hay recorte de trazabilidad. Si el conjunto de nombres
raros supera 5.000, \code{cobertura_diagnosticos} declara el recorte y su limite.
Quien decida automáticamente sobre un perfil debe revisar
\code{nrow(perfil$cobertura_diagnosticos)} además de las severidades: un perfil
sin hallazgos y con diagnósticos no evaluados no es un perfil limpio.
Cuando una clave declarada no queda plenamente verificada, \code{meta$clave}
conserva los estados de unicidad y ausencia de nulos, sus conteos y la
semántica usada por la trazabilidad. Los tres responden preguntas distintas
y no comparten universo:
\itemize{
\item \code{unicidad} se evalúa \strong{sólo entre las filas con la clave completa}
(\code{semantica = "claves_completas"}), porque una repetición entre filas
incompletas no viola la unicidad: en SQL dos \code{NULL} no son iguales.
\code{filas_evaluadas} cuenta esas filas y \code{filas_totales} la tabla entera. Su
estado es \code{"verificada"}, \code{"refutada"}, \code{"no_verificada"} cuando no se
pudo comparar, o \code{"sin_casos_evaluables"} cuando \strong{ninguna} fila tiene la
clave completa: ahí la unicidad sería cierta sobre un conjunto vacío, que
es cierto y engañoso a la vez, y por eso tiene estado propio.
\item \code{ausencia_nulos} responde si todos los componentes están presentes.
\item \code{trazabilidad} conserva la semántica de R, que es la que localiza las
filas, e informa en \code{colisiona_con_ausentes} si el localizador queda
ambiguo porque dos filas con ausentes comparten representación.
}
}
\description{
Examina un \code{data.frame}, \code{tibble} o \code{data.table} y devuelve estadísticas
generales, métricas por columna, patrones, formatos de fecha y hallazgos
accionables. Todas las proporciones se expresan en \verb{[0, 1]}.
}
\details{
Los umbrales de faltantes se aplican a la suma de ausentes reales y
faltantes disfrazados y son estrictos: la proporción debe superar el umbral
para generar el nivel correspondiente. La lista de cadenas está congelada con referencia a
\href{https://github.com/njtierney/naniar}{naniar}::common_na_strings 1.1.0 y suma extensiones habituales en datos
administrativos uruguayos. Las entradas que \href{https://github.com/njtierney/naniar}{naniar} expresa como patrones
escapados se adaptan a los signos literales de interrogación, asterisco y
punto porque aquí se comparan por igualdad. La lista no depende de la
versión instalada.
Los sentinelas numéricos predeterminados son \code{-9}, \code{-99}, \code{-999}, \code{-9999} y
\code{999}. La lista es deliberadamente más corta que
\href{https://github.com/njtierney/naniar}{naniar}::common_na_numbers 1.1.0:
\code{66}, \code{77}, \code{88} y \code{9999} también pueden
ser edades, códigos o años legítimos. \code{sentinelas_numericos} representa la
política completa, no una lista que se agrega silenciosamente: use
\code{numeric()} para desactivar todos los sentinelas numéricos, o
\code{sentinelas_naniar} para solicitar explícitamente la lista de naniar.

\code{muestra} limita sólo el descubrimiento de patrones, la inferencia de tipos y
la detección de formatos de fecha. Las demás métricas y hallazgos se calculan
sobre todas las filas. Por eso \code{meta$filas_analizadas} describe el máximo
usado por los análisis muestreados, no el alcance del perfil completo.
En cada fila de \code{columnas}, \code{n_filas_analizadas_tipo} y
\code{muestreado_tipo_inferido} declaran el alcance concreto de
\code{proporcion_tipo_inferido}; no debe interpretarse esa proporción como si
hubiera usado necesariamente toda la columna.

En una columna temporal, \code{minimo}, \code{maximo}, \code{media} y \code{mediana} quedan en
\code{NA} y su valor viaja en \code{minimo_fecha}, \code{maximo_fecha}, \code{media_fecha} y
\code{mediana_fecha}, que son texto legible. \code{desvio} es la excepción y conviene
saberlo: no es un momento sino una duración, así que se informa como número,
y ese número está \strong{en segundos} tanto para \code{"fecha"} como para
\code{"fecha-hora"}, porque las dos clases se unifican en esa unidad antes de
resumirlas. Un desvío de \code{136610.4} sobre una columna de fechas son 1,6 días.

Ese detalle importa al comparar las dos puertas. \code{\link[=perfilar_dbi]{perfilar_dbi()}} no clasifica
tipos: informa el que declara el motor, y un motor que no preserva \code{DATE} ni
\code{BOOLEAN} —SQLite guarda ambos como número— hace que esas columnas se midan
como números. La misma columna de fechas da entonces \code{desvio} en días por la
puerta DBI y en segundos por ésta, y su \code{moda} sale como el entero crudo del
motor en vez de la fecha formateada. No es una discrepancia de cálculo: cada
puerta describe lo que tiene delante, y lo que tiene delante es distinto.

Una columna cuyo año se expresa con dos dígitos se informa con su
\code{tipo_inferido} —\code{"fecha"} o \code{"fecha-hora"}— pero deja \code{minimo_fecha},
\code{maximo_fecha}, \code{media_fecha} y \code{mediana_fecha} en \code{NA}. No es una omisión:
\code{23} puede ser 1923 o 2023, y elegir el siglo para calcular un rango sería
inventarlo. El hallazgo \code{anio_de_dos_digitos} señala esas columnas, y el
rango aparece una vez que el usuario resuelve la ambigüedad.
Una columna de períodos expresados sólo como mes y año informa la
\code{granularidad} \code{"mes"} en \code{formatos_fecha} y deja los resúmenes de fecha en
\code{NA} con \code{estado_resumen_cuantitativo = "granularidad_incompleta"}: asignar
el día 1 para obtener un mínimo o una media también sería inventar un dato.
Si esos períodos son minoritarios dentro de una columna que también contiene
fechas con día, los resúmenes se calculan sólo sobre las fechas completas y
declaran \code{estado_resumen_cuantitativo = "calculados_sobre_dias"}, junto con
\code{n_fechas_resumidas} y \code{n_fechas_excluidas_granularidad}. El mínimo y el máximo
son entonces condicionales al subconjunto con día; no representan un rango
de toda la columna.

Para números ordinarios, los estadísticos cuantitativos se calculan sólo con
valores finitos; \code{n_nan}, \code{n_infinito_positivo} y \code{n_infinito_negativo}
declaran lo excluido. En columnas \code{integer64} que exceden el entero máximo
representable exactamente por \code{double}, \code{minimo} y \code{maximo} quedan en \code{NA} y
los extremos exactos se conservan en \code{minimo_exacto} y \code{maximo_exacto}.
Una columna de listas intenta contar sus valores distintos; si la clase no
admite comparación, informa \code{NA} en lugar de afirmar cero.
Las columnas matriciales se conservan como una unidad por fila: \code{n} informa
las filas de la tabla, pero los estadísticos por valor quedan en \code{NA} y un
hallazgo explica que deben separarse en columnas con semántica explícita.

La ley de Benford se evalúa sólo en columnas numéricas con al menos 50
valores finitos, para no agregar cobertura a columnas que ni siquiera son
candidatas. Antes de comparar exige variación, que la columna no parezca un
identificador ni una secuencia correlativa, al menos 100 observaciones
positivas utilizables, una proporción de positivos igual a 1 y tres órdenes
de magnitud según \code{log10(max/min)}. Si falla alguna precondición no emite un
hallazgo: la enumera en \code{cobertura_diagnosticos}. Si aplica,
\code{meta$benford$resultados} conserva la distribución observada y esperada por
primer dígito, el chi-cuadrado de Pearson, ocho grados de libertad y el valor
p; \code{meta$benford$umbrales} publica todos los cortes. Un valor p menor que
\code{0.01} genera \code{desviacion_benford} como señal descriptiva para revisar, no
como evidencia de fraude o manipulación. Topes administrativos, redondeos,
precios psicológicos y subsidios de monto fijo son explicaciones posibles.

Las relaciones aritméticas se buscan sólo entre columnas numéricas
declaradas y con variación: \code{Date}, \code{POSIXt}, \code{difftime}, \code{integer64}, texto
numérico y columnas constantes no participan. Cada relación requiere al
menos tres filas con valores finitos en todas las columnas involucradas;
los \code{NA}, \code{NaN} e infinitos quedan fuera del universo que publica la
evidencia. Para cada terna se prueban las tres orientaciones de una identidad
aditiva; esto cubre sumas y sus restas equivalentes sin informar tres veces
la misma igualdad. En pares proporcionales, \code{k} es la mediana de los cocientes
finitos cuya base no es cero, pero el cumplimiento se evalúa después también
en las filas con base cero. Si una identidad aditiva ya relaciona una terna,
se omiten las proporcionalidades redundantes entre su total y sus sumandos;
se conserva la proporcionalidad entre los dos sumandos. Una regularidad
completa se informa con severidad \code{"ok"}; si alcanza el umbral pero tiene
discrepancias, sigue el criterio de las relaciones de orden y es
\code{"sospechoso"}. Todo esto describe evidencia observada: no declara una regla
del dominio ni autoriza una corrección.
Los valores de texto que no forman UTF-8 válido tampoco se convierten: se
cuentan, se excluyen de los análisis textuales y generan un hallazgo con sus
posiciones. Los diagnósticos de invisibles incluyen controles C0/C1,
espacios Unicode, marcas direccionales, BOM y otros caracteres de transporte.
La evidencia los muestra como puntos de código; los espacios Unicode se
detectan aunque sólo se normalizan mediante una acción explícita, y ZWJ/ZWNJ
se informan pero se conservan porque pueden ser semánticos. La comparación
de duplicados con \code{normalizar = TRUE} aplica estas mismas clases sin borrar
ZWJ/ZWNJ.

Los resúmenes de fecha-hora se expresan siempre en UTC y llevan el sufijo
\code{UTC} en el texto para hacer visible la zona aplicada. El instante se
conserva aunque la columna de entrada use otra zona horaria. Las columnas
\code{POSIXt} declaran \code{zona_horaria_origen} y
\code{n_filas_fecha_civil_distinta_utc}, que cuenta filas cuya fecha civil cambia
al mostrar el instante en UTC. Cuando la fecha civil cambia se emite un
hallazgo \code{zona_horaria_fecha_hora}; si la zona de origen no está declarada,
el conteo queda en \code{NA} y \code{cobertura_diagnosticos} declara que no se evaluó.
La zona se declara en la columna original, por ejemplo:
\code{attr(x, "tzone") <- "America/Montevideo"}.

El diagnóstico de formas Unicode compara sin modificar el texto y puede usar
el paquete opcional \code{stringi} para enriquecer la evidencia cuando existen
caracteres no ASCII. \code{columnas$unicode_evaluado} declara si esa comprobación
pudo ejecutarse; en texto no ASCII sin \code{stringi} queda \code{FALSE},
\code{n_variantes_unicode} queda en \code{NA} y \code{cobertura_diagnosticos} informa la
dependencia ausente. Las columnas ASCII se evalúan siempre y conservan cero.
El perfil de comparación es completamente R base.

Las columnas \code{sfc} declaran su CRS, los tipos concretos y la dimensión
(\code{XY}, \code{XYZ}, \code{XYM} o \code{XYZM}), además de geometrías vacías, validez, dominio y
caja envolvente. \code{POINT}/\code{MULTIPOINT}, \code{LINESTRING}/\code{MULTILINESTRING} y
\code{POLYGON}/\code{MULTIPOLYGON} son una misma familia: el hallazgo de tipos mixtos
aparece sólo al combinar familias o ante \code{GEOMETRYCOLLECTION}.
La validez se calcula siempre con GEOS en el plano, sin CRS, para que el
resultado no dependa de que \code{s2} esté instalado; \code{validez_criterio} publica
\code{"planar"} y \code{n_validez_evaluados} publica su universo, que incluye las
geometrías vacías porque GEOS sí devuelve su validez. Si hay una dimensión
M, se aplica \code{sf::st_zm()} y se valida la topología XY;
\code{validez_preprocesamiento = "st_zm(x)"} declara esa transformación. Si el
CRS es geográfico, un fallo es \code{sospechoso} y no un \code{error}, porque no afirma
invalidez esférica. Las dimensiones Z y M no se evalúan como medidas:
\code{dimensiones_no_evaluadas} las enumera y \code{cobertura_diagnosticos} deja
constancia explícita incluso cuando la validez XY sí pudo calcularse.
En un CRS geográfico el dominio exige longitudes en \verb{[-180, 180]} y latitudes
en \verb{[-90, 90]}. Además, las coordenadas se comparan en longitud/latitud con
la \code{BBOX} del área de uso incluida en el WKT del CRS. Este control puede
detectar valores en unidades incompatibles —por ejemplo, grados declarados
como metros—, pero no una zona UTM equivocada cuando esa interpretación cae
dentro del área de la zona declarada. Una \code{BBOX} mundial es un no-op válido;
si el WKT no permite extraer la caja, el dominio queda en \code{NA} y
\code{cobertura_diagnosticos} lo declara en lugar de suponer el mundo entero.
Las geometrías vacías se cuentan aparte y no integran el universo del dominio
ni de la bbox. \code{n_dominio_evaluados} y \code{n_bbox_evaluados} publican ambos
universos; la bbox se calcula sobre coordenadas crudas de geometrías no
vacías, incluidas las que el dominio marque fuera, y \code{bbox_alcance} lo
declara. Si todas son vacías, sus conteos evaluados y fuera de dominio son
cero. Sin CRS,
\code{n_fuera_de_dominio} queda en \code{NA} y se emite \code{crs_no_declarado}: nunca se
supone EPSG:4326. Si falta el paquete opcional \code{sf}, todos esos campos quedan
en \code{NA}, no se emite un hallazgo geométrico y \code{cobertura_diagnosticos}
registra la dependencia ausente.
El argumento \code{normalizar} declara el perfil de comparación que se conserva
en \code{meta$normalizacion}; cambia sólo la representación usada para comparar,
no el texto guardado. \code{TRUE} usa el perfil predeterminado, \code{FALSE} desactiva
sus pasos configurables, \code{"amplio"} activa los tres pliegues optativos y
\code{\link[=normalizacion]{normalizacion()}} permite declararlos. También admite una lista nombrada por
columna. \code{meta$normalizacion_fusiones} informa, para cada paso activo, la
diferencia entre los valores distintos con el perfil completo y los valores
distintos con ese paso apagado y todo lo demás igual. Esa comparación
responde cuánto aporta cada paso por separado; sus números no son aditivos
porque dos pasos pueden fundir el mismo par. \code{n_distintos} y
\code{n_distintos_normalizados} declaran el total antes y después del perfil
completo. La descomposición canónica es siempre activa y forma parte de la
línea base, no una fila configurable.
Con \code{normalizar = FALSE} no hay pasos configurables que medir y el informe
de fusiones queda en \code{NULL}; en un perfil por columna sólo se incluyen las
columnas con algún paso activo. Si \code{detectar_duplicados_aproximados()} recibe
un perfil, puede reutilizar este informe ya calculado.
El informe usa el vocabulario completo: \code{n_distintos} y \code{n_usados} son el
número de valores distintos realmente comparados y el estado es \code{exacto}.
Las fusiones son una propiedad de pares, por lo que muestrear valores
aislados podría dejar fuera los dos miembros de cada par y convertir una
fusión real en un cero falso. La normalización se vectoriza para que este
alcance completo no dependa de la cardinalidad de la columna.
Las columnas sin ausentes que tienen al menos 100 filas y 90 \% de valores
distintos producen un hallazgo \code{casi_clave} cuando el valor dominante
concentra al menos la mitad de los duplicados excedentes. La concentración
se calcula sobre las repeticiones posteriores a la primera de cada valor, no
sólo sobre la tasa de distintos. La evidencia declara el mínimo de filas,
ambos umbrales, los valores que colisionan y sus frecuencias. Las variables
con rol propuesto \code{fecha}, incluidas fecha-hora, se excluyen. Así una colisión
concentrada queda separada del texto libre de alta cardinalidad con
repeticiones dispersas.
Un vector \code{double} sólo participa si todos sus valores finitos son enteros;
así se conservan identificadores importados con ese almacenamiento y se
excluyen medidas con alguna parte fraccionaria, como importes o coordenadas.
Los vectores \code{integer64} se tratan como enteros semánticos. La evidencia del
hallazgo declara este criterio y el recuento observado.
Además, si \code{casi_duplicados_vocabulario = TRUE}, el perfil busca variantes
casi duplicadas en columnas de texto. Agrupa el vocabulario crudo mediante
fusiones exactas de la normalización y estrellas de distancia centradas en
un valor de frecuencia estrictamente mayor y único; los empates no se fuerzan.
No cierra cadenas transitivamente ni elige una forma canónica. La unidad es
el valor distinto, no la fila, y cada variante
conserva su frecuencia. Cada grupo declara sus distancias mínima y máxima.
El límite \code{max_proporcion_grupo_vocabulario} evita presentar un grupo que
abarque casi toda la columna como un diagnóstico útil: en ese caso el alcance
dice que el diagnóstico no aplica. Ese límite se activa desde 20 valores
distintos o cuando el grupo mayor ya tiene 10 variantes; sólo suprime el
grupo si además ocupa una fracción mayor que el umbral. Así un grupo de tres
en cuatro valores se entrega, pero quince variantes que ocupan toda una
columna no se presentan como una sola familia. Un grupo grande dentro de un
vocabulario mucho mayor puede seguir pasando si su proporción es pequeña. El
alcance expone ambos cortes. El argumento permite apagar el detector cuando
no corresponde a la tabla. Si hay pares cercanos pero todas las frecuencias
empatan, el alcance declara que no hubo asimetría para formar una estrella y
sugiere \code{\link[=detectar_duplicados_aproximados]{detectar_duplicados_aproximados()}} para comparar filas. El alcance
clasifica cada grupo como \code{normalizacion_exacta}, \code{dentro_de_palabra},
\code{token_completo}, \code{token_unico}, \code{mixta} o \code{indeterminada}. La primera indica
una coincidencia tras normalizar; \code{dentro_de_palabra}, una diferencia dentro
de un token; \code{token_completo}, diferencias en tokens completos;
\code{token_unico}, que ambos valores son un único token y la clase estructural no
aplica; \code{mixta}, aristas de más de una clase; e \code{indeterminada}, que no hubo
aristas clasificables. Son evidencia descriptiva, no una decisión sobre
identidad. El agrupamiento no cambia por esa etiqueta. El alcance
también descarta aristas de distancia cuyos números no coinciden. Se comparan
las secuencias numéricas, quitando ceros de relleno y separadores de miles;
una diferencia numérica se trata como otra entidad. Esto puede dejar sin
agrupar una errata dentro de un número, porque no hay evidencia para
distinguirla de dos entidades reales. El alcance declara los valores y pares
comparados, los recortes por cardinalidad y si \code{stringdist} no estuvo
disponible; también informa cuántas aristas se descartaron por esa regla y
el tamaño compatible después del filtro. El tamaño máximo usado para el
límite conserva el componente potencial antes del filtro numérico, para que
una familia de entidades numeradas no vuelva a presentarse como una sola
variante; el tamaño compatible se muestra aparte. Para mantener acotado el
perfil, por omisión se evalúan hasta
5.000 valores distintos y 2.000.000 de pares de unidades normalizadas; si
se alcanza un límite, la evidencia lo declara y no presenta el resultado
como universo completo. Las fusiones exactas se informan aun sin ese paquete
opcional.
Antes de formar esos grupos se retiran los valores que el mismo perfil ya
informó como \code{faltantes_disfrazados}. El diagnóstico fuerte de ausencia tiene
precedencia: un centinela no se presenta también como posible variante de un
valor válido. El alcance declara cuántas observaciones retiró este filtro.

La clasificación de posibles datos personales es más amplia que la
protección. Cada clasificación declara \code{poder_discriminante} y \code{proteger}:
\itemize{
\item \code{debil}: una forma genérica, como siete a doce dígitos, coincide también
con importes, facturas y códigos; se informa pero no se ocultan valores;
\item \code{medio}: el nombre de la columna expresa una categoría personal (por
ejemplo \code{telefono} o \code{fecha_nacimiento}); se protege aunque sus valores no
se puedan validar. El nombre tiene prioridad sobre una forma numérica
genérica y también determina la etiqueta de tipo;
\item \code{alto}: una forma muy específica, como un correo, o nombre y forma se
apoyan mutuamente; se protege;
\item \code{verificado}: al menos tres valores distintos y al menos el 90\% cumple uno
de los validadores personales configurados; se protege incluso sin un
nombre orientador. El pack uruguayo es el predeterminado, pero puede
reemplazarse por un \code{pack_validadores()} de otro país o desactivarse con
\code{FALSE}. La tolerancia del 10\% permite tipeos aislados sin convertir una
columna real en una salida pública; el umbral es configurable.
}

La forma genérica de siete a doce dígitos tiene poder discriminante débil:
también describe importes, teléfonos, facturas e identificadores. Las formas
con separadores sólo se aceptan cuando tienen una estructura de documento
reconocible (por ejemplo, una cédula con grupos y guion o un RUT con grupos
de tres y cuatro dígitos); una fecha ISO, una fecha con puntos o guiones y
separadores arbitrarios no se consideran documentos. Un validador de dígito
que supera el umbral aporta evidencia verificable y eleva la clasificación;
una forma sola nunca se trata como prueba de identidad.

Este criterio mide capacidad de discriminación, no juzga si la presencia del
dato es correcta. La protección sustituye modas, ejemplos, evidencia y
extremos o medianas que corresponden a observaciones reales. Las medias y
desvíos se conservan como síntesis no ligadas a una fila;
\code{detalle_proteccion_personal} hace visible la supresión. En fechas de
nacimiento, un hallazgo separado conserva el diagnóstico de valores
anteriores a 1900 o posteriores a la corrida sin publicar las fechas.
Los números escritos como texto reconocen tanto coma como punto decimal y
sus separadores de miles simétricos. Los prefijos de tres letras separados
del número, incluso como sufijo, \code{U$S} y los símbolos monetarios se
conservan como evidencia; \code{monedas_mixtas} informa sus frecuencias sin
convertir ni suponer tasas de cambio. Una única moneda o un símbolo \code{$}
aislado no produce ese hallazgo.
Un sufijo de unidad se reconoce sólo si es \verb{\%} o una abreviatura alfabética
en minúsculas; por eso \verb{12 kg} y \verb{13500 g} son unidades, mientras que
\verb{12A} y \verb{13B} se tratan como códigos. Si hay más de una unidad observada,
\code{unidades_mixtas} informa sus frecuencias y no convierte ni compara sus
magnitudes. Una única unidad no genera ese hallazgo.
\code{celdas_multivaluadas} es deliberadamente conservador: usa los patrones de
\code{\link[=descubrir_patrones]{descubrir_patrones()}} y exige partes numéricas, alfanuméricas o
identificadoras puntuadas homogéneas, compatibles con el patrón del resto de
la columna. No interpreta comas en nombres o direcciones como listas; el
delimitador, la cantidad de celdas y la distribución de valores por celda
quedan en la evidencia del hallazgo.
}
\examples{
perfil <- perfilar(datos_administrativos)
perfil
summary(perfil)
}
\seealso{
\code{\link[=descubrir_patrones]{descubrir_patrones()}}, \code{\link[=detectar_dependencias]{detectar_dependencias()}},
\code{\link[=proponer_modelo]{proponer_modelo()}}, \code{\link[=planificar_limpieza]{planificar_limpieza()}}
}
