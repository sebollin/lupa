% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profiling-avanzado.R
\name{analizar_tiempo}
\alias{analizar_tiempo}
\title{Examinar regularidad y cobertura temporal}
\usage{
analizar_tiempo(
  datos,
  perfil = NULL,
  columnas = NULL,
  calendario = 1:7,
  frecuencia_dias = NULL,
  max_huecos = 20L,
  max_columnas = 50L
)
}
\arguments{
\item{datos}{Tabla que se desea examinar.}

\item{perfil}{Perfil opcional de los mismos datos.}

\item{columnas}{Columnas temporales; \code{NULL} usa clases e inferencia.}

\item{calendario}{Días de semana esperados, enteros entre 1 y 7.}

\item{frecuencia_dias}{Frecuencia entera conocida en días. Si es \code{NULL}, se
propone la moda de los intervalos positivos.}

\item{max_huecos}{Máximo de grupos de huecos devueltos por columna.}

\item{max_columnas}{Máximo de columnas temporales analizadas.}
}
\value{
Objeto \code{analisis_temporal} con \code{resumen}, \code{dias_semana}, \code{huecos} y
\code{propuestas}. El recorte de huecos queda en \code{resumen}; el de columnas, en
atributos del objeto. \code{resumen} agrega tres campos sobre el alcance del
resumen, con el mismo vocabulario que usa \code{\link[=perfilar]{perfilar()}} sobre la misma
columna: \code{n_fechas_excluidas_parseo} cuenta los valores \strong{presentes} que
ningún formato confirmado pudo convertir y que por eso quedaron fuera;
\code{n_fechas_excluidas_granularidad}, cuántos de ésos son períodos de mes —
\code{2024-02} y sus formas—, que no se convierten a propósito porque no
nombran un día; y \code{estado_resumen} vale \code{"calculados_sobre_dias"} cuando
hubo períodos de mes, \code{"calculados_sobre_fechas_parseadas"} cuando sólo
hubo valores ilegibles, y \code{"calculados"} cuando no quedó nada afuera. Los
tres se informan siempre, haya muestreo o no, porque el descarte tampoco
depende del muestreo. Un \code{NA} no entra en esta cuenta: es una ausencia
declarada y se informa como faltante, no como valor que no se pudo leer.

Una columna mixta —días y meses— \strong{sí} se resume, sobre sus fechas
completas. Sólo cuando ningún formato confirmado nombra un día no hay serie
diaria que construir; entonces la columna no aparece en \code{resumen}, y se
declara en los atributos \code{columnas_omitidas} y \code{columnas_sin_serie_diaria}
en vez de desaparecer.
}
\description{
Para cada columna temporal propone una frecuencia en días. La confianza es
el mínimo entre la contigüidad de las fechas sobre la grilla propuesta y la
cobertura del período: una coincidencia breve dentro de una serie muy
dispersa no puede producir confianza alta. Ambas componentes se devuelven
para que la propuesta sea auditable. La propuesta nunca queda confirmada
automáticamente. \code{calendario} usa días
ISO: 1 es lunes y 7 domingo; así una oficina puede declarar \code{1:5} sin que la
ausencia de fines de semana se interprete como hueco. Las fechas-hora se
llevan a fecha civil en UTC para que el resultado no dependa de la zona del
equipo que ejecuta el análisis.
}
\examples{
fechas <- as.Date("2026-01-01") + c(0:4, 20:24)
analizar_tiempo(data.frame(fecha = fechas))
}
\seealso{
\code{\link[=detectar_formatos_fecha]{detectar_formatos_fecha()}}, \code{\link[=analizar]{analizar()}}
}
