% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/organizacion.R
\name{organizacion}
\alias{organizacion}
\title{Declarar una organización y las colecciones que le pertenecen}
\usage{
organizacion(nombre, colecciones)
}
\arguments{
\item{nombre}{Nombre de la organización. Es su identidad dentro de un
conjunto de organizaciones.}

\item{colecciones}{Colecciones que le pertenecen: un vector de nombres, o una
lista de objetos de \code{\link[=coleccion]{coleccion()}} o \code{\link[=perfilar_coleccion]{perfilar_coleccion()}}. Si la lista
tiene nombres, esos nombres mandan sobre el del objeto.}
}
\value{
Objeto S3 \code{organizacion_lupa} con \code{nombre}, \code{declaradas} y
\code{n_declaradas}.
}
\description{
Las granularidades novena y décima del marco —una organización y un conjunto
de organizaciones— necesitan un dato que \strong{no está en los datos}: qué bases
pertenecen a qué organismo. \code{lupa} no lo adivina, así que lo declara quien lo
sabe, con el mismo mecanismo que ya usa el conjunto de colecciones.
}
\details{
\strong{Es opcional.} Un análisis de calidad no siempre tiene una organización
detrás, y nada obliga a pasar por estos niveles: existen para quien los
necesita. Sin declaración, \code{\link[=agregar]{agregar()}} a \code{"organizacion"} se niega y explica
cómo declararla, que es distinto de inventar una frontera que nadie nombró.

No pide una conexión. Una colección es una cosa viva —tablas de un motor—,
pero una organización es un enunciado \emph{sobre} colecciones, y puede reunir
colecciones medidas en momentos distintos o contra motores distintos.
}
\examples{
organismo <- organizacion("MIDES", c("padron", "tramites"))
organismo
organismo$declaradas
}
\seealso{
\code{\link[=agregar]{agregar()}}, \code{\link[=coleccion]{coleccion()}}, \code{\link[=granularidades]{granularidades()}}
}
