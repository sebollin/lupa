% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profiling-avanzado.R
\name{distribucion_valores}
\alias{distribucion_valores}
\title{Distribuciones de valores y cuantiles por columna}
\usage{
distribucion_valores(
  datos,
  perfil = NULL,
  max_valores = 20L,
  probabilidades = c(0, 0.25, 0.5, 0.75, 1),
  muestra = 1e+05,
  proteger_datos_personales = TRUE
)
}
\arguments{
\item{datos}{Tabla que se desea examinar.}

\item{perfil}{Perfil opcional de los mismos datos; evita repetir la
clasificación de posibles datos personales.}

\item{max_valores}{Máximo de valores mostrados por columna.}

\item{probabilidades}{Probabilidades de los cuantiles, en \verb{[0, 1]}.}

\item{muestra}{Máximo de filas por columna; \code{Inf} desactiva el muestreo.}

\item{proteger_datos_personales}{Si se ocultan valores de columnas cuya
clasificación activa protección automática. Véase \code{\link[=perfilar]{perfilar()}}.}
}
\value{
Objeto \code{distribuciones_perfil}, una lista con data frames
\code{frecuencias}, \code{cuantiles} y \code{alcance}. Todas las proporciones están en
\verb{[0, 1]}.
}
\description{
Resume frecuencias sin conservar una tabla completa de alta cardinalidad.
Cada columna se limita a \code{max_valores}; \code{alcance} declara cuántos valores se
analizaron, cuántos distintos se observaron y si hubo muestreo o truncamiento.
Los cuantiles se calculan sólo para números ordinarios finitos.
}
\details{
Cuando una columna tiene evidencia suficiente para activar la protección de
datos personales, sus frecuencias y niveles se conservan pero el valor
concreto se reemplaza. Los cuantiles
mantienen sus filas y probabilidades, pero \code{valor} queda en \code{NA} y \code{estado}
informa \code{"valor_protegido"}: un cuantil, especialmente en tablas pequeñas,
puede coincidir exactamente con una observación. Esta protección es
independiente de la usada al construir el perfil.
}
\examples{
d <- data.frame(grupo = c("A", "A", "B"), valor = c(1, 2, 10))
distribucion_valores(d)
}
\seealso{
\code{\link[=perfilar]{perfilar()}}, \code{\link[=analizar]{analizar()}}, \code{\link[=clasificar_variables]{clasificar_variables()}}
}
