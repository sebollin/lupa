% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/requisitos.R
\name{requisitos_motor}
\alias{requisitos_motor}
\title{Consultar requisitos para conectarse a motores de bases}
\usage{
requisitos_motor(motor = NULL)
}
\arguments{
\item{motor}{\code{NULL} para devolver el catálogo completo, o el nombre de un
motor, como \code{"oracle"}, \code{"postgresql"} o \code{"sql server"}.}
}
\value{
Un \code{data.frame} de clase \code{requisitos_motor}, con una fila por variante
documentada. Incluye el estado comprobado del paquete R y un estado explícito
para la biblioteca del sistema: \code{no_comprobada} no significa instalada.
}
\description{
Devuelve el catálogo de paquetes R, bibliotecas del sistema, dialectos y
estado de prueba que \code{lupa} conoce. La presencia del paquete R se comprueba
en la máquina actual. La biblioteca del sistema no se declara instalada sin
una comprobación específica: cuando corresponde, la columna lo deja como
\code{no_comprobada} y conserva las rutas de resolución posibles.
}
\details{
El estado \code{probado} se copia de la tabla de motores de los README. \code{esperado}
significa que el dialecto está implementado pero no se comprobó contra ese
motor real en este repositorio. Los motores que no figuran en esa tabla no se
presentan como probados.
}
