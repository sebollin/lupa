% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historico.R
\name{detectar_deriva_calidad}
\alias{detectar_deriva_calidad}
\title{Detectar deriva en una serie de evaluaciones}
\usage{
detectar_deriva_calidad(historico, nivel = c("perfil", "regla"), umbral = 0.05)
}
\arguments{
\item{historico}{Objeto creado por \code{\link[=historico_calidad]{historico_calidad()}}.}

\item{nivel}{\code{"perfil"} o \code{"regla"}.}

\item{umbral}{Cambio absoluto mínimo considerado significativo. El valor
predeterminado de \code{0.05} representa cinco puntos porcentuales: evita tratar
como deriva diferencias de redondeo, pero sigue siendo sensible a cambios
operativamente visibles.}
}
\value{
Data frame \code{deriva_calidad} con una fila por par de corridas
consecutivas. Una mejora significativa conserva severidad \code{ok}; un
deterioro de al menos un umbral es \code{sospechoso} y uno de al menos dos
umbrales es \code{error}. \code{identidad_tabla} separa series de tablas distintas y
\code{aspecto} marca el resultado o un cambio de configuración; este último se
informa como \code{error} pero no suprime la comparación.

\strong{Un par que no se puede comparar no recibe veredicto.} Si alguna de las
dos corridas no evaluó su resultado, \code{delta}, \code{cambio_absoluto},
\code{significativo}, \code{direccion} y \code{severidad} quedan en \code{NA} —no en \code{estable}
ni en \code{ok}— y \code{descripcion} nombra de qué lado falta el resultado. Filtrar
por \code{severidad != "ok"} deja fuera esas filas a propósito: no son filas
sanas, son filas sin medición, y se encuentran con \code{is.na(significativo)}.
}
\description{
Compara corridas consecutivas, ordenadas por fecha dentro de cada perfil o
regla, y marca cambios significativos en la escala \verb{[0, 1]}. Los empates de
fecha se resuelven por los bytes de \code{id_medicion}, de modo que la misma
secuencia persiste igual bajo cualquier locale.
}
\examples{
# El ejemplo de historico_calidad() muestra cómo construir las corridas.
}
