% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metodos-perfil.R
\name{accesores_perfil}
\alias{accesores_perfil}
\alias{hallazgos}
\alias{columnas}
\alias{cobertura}
\alias{n_filas}
\alias{sql_perfil}
\title{Leer un perfil sin depender de su forma}
\usage{
hallazgos(x, ...)

columnas(x, ...)

cobertura(x, ...)

n_filas(x, ...)

sql_perfil(x, ...)
}
\arguments{
\item{x}{Objeto de \code{\link[=perfilar]{perfilar()}}, \code{\link[=analizar]{analizar()}}, \code{\link[=perfilar_dbi]{perfilar_dbi()}} o
\code{\link[=perfilar_coleccion]{perfilar_coleccion()}}.}

\item{...}{Sin uso, para compatibilidad de métodos.}
}
\value{
\code{hallazgos()} y \code{columnas()} devuelven \code{data.frame}; \code{cobertura()}
devuelve la tabla de diagnósticos no evaluados; \code{n_filas()} devuelve el
conteo de filas del alcance, o \code{NA} con su motivo cuando el objeto no lo
conoce.
}
\description{
\code{perfilar()}, \code{perfilar_dbi()} y \code{perfilar_coleccion()} devuelven objetos con
formas distintas, y eso convertía la lectura en un acertijo:
\code{perfil$general$filas} funciona sobre la salida en memoria y devuelve \code{NULL}
sobre la salida DBI, donde el conteo vive en \code{resumen_tabla$meta$filas}. Un
\code{NULL} silencioso en un guion de medición es la peor forma de fallar: no
avisa, y lo que sigue calcula sobre nada.
}
\details{
Estos accesores no agregan información: le ponen un solo nombre a la que ya
está. La forma concreta de cada objeto sigue disponible y documentada.

\strong{Lo que no hay, no se inventa.} Un \code{perfilar_dbi()} sin muestra leída no
tiene hallazgos por fila, y \code{hallazgos()} devuelve una tabla vacía \strong{con su
aviso}, no una tabla que aparente que se midió y no había nada.
}
\examples{
perfil <- perfilar(datos_administrativos, analizar_dependencias = FALSE)
nrow(hallazgos(perfil))
n_filas(perfil)
nrow(cobertura(perfil))
}
\seealso{
\code{\link[=perfilar]{perfilar()}}, \code{\link[=perfilar_dbi]{perfilar_dbi()}}, \code{\link[=perfilar_coleccion]{perfilar_coleccion()}}
}
