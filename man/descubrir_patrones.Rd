% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patrones.R
\name{descubrir_patrones}
\alias{descubrir_patrones}
\title{Descubrir patrones de formato}
\usage{
descubrir_patrones(
  x,
  distinguir_mayusculas = TRUE,
  expandir = FALSE,
  max_patrones = 20,
  na.rm = TRUE,
  muestra = 1e+05,
  umbral_raro = 0.05
)
}
\arguments{
\item{x}{Vector que se convertirá a texto.}

\item{distinguir_mayusculas}{Si es \code{TRUE}, distingue \code{a} de \code{A}.}

\item{expandir}{Si es \code{FALSE}, colapsa tokens repetidos (\code{9999} a \verb{9+}).}

\item{max_patrones}{Número máximo de patrones que se muestran.}

\item{na.rm}{Si es \code{TRUE}, excluye los valores ausentes.}

\item{muestra}{Máximo de valores que se analizan.}

\item{umbral_raro}{Umbral usado para conservar un resumen acotado de
patrones raros para los hallazgos.}
}
\value{
Un data frame de clase \code{patrones} con patrón, frecuencia, proporción
y ejemplos. Los atributos \code{total}, \code{analizados}, \code{filas_analizadas} y
\code{muestreado} describen el posible muestreo; \code{filas_analizadas} es un alias
explícito de \code{analizados} para mantener el alcance visible junto a otros
diagnósticos. \code{resumen_patrones} conserva sólo el patrón dominante
y hasta seis patrones raros para presentacion; nunca guarda la distribucion
completa. \code{patrones_raros_trazabilidad} conserva solo los nombres de los
patrones raros, hasta 5.000, para que la trazabilidad pueda enumerar filas
sin retener frecuencias ni ejemplos. Las proporciones siempre estan en
\verb{[0, 1]}. \code{n_patrones_distintos} registra el total antes de truncar la tabla
para informar omisiones sin retenerla. \code{n_patrones_raros} y
\code{n_patrones_raros_trazabilidad} registran cuantos patrones raros habia antes
de sus respectivos limites. \code{n_filas_patrones_no_dominantes_excluidos}
registra cuantas filas pertenecen a patrones no dominantes cuya proporcion
no es rara y que por eso quedan fuera del hallazgo.
}
\description{
Generaliza un vector de texto mediante la convención del \emph{Pattern Finder}
de DataCleaner: \code{9} representa un dígito, \code{a} una letra minúscula y \code{A} una
letra mayúscula. Los símbolos y espacios se conservan literalmente.
}
\details{
El cálculo aplica reemplazos vectorizados sobre el vector completo. Si el
vector supera \code{muestra}, usa una muestra sistemática reproducible y registra
esa decisión en los atributos del resultado.
}
\examples{
descubrir_patrones(
  c("2020-01-31", "2021-12-01", "31/01/2020"),
  expandir = TRUE
)
}
\seealso{
\code{\link[=perfilar]{perfilar()}}, \code{\link[=inferir_tipo]{inferir_tipo()}}, \code{\link[=detectar_formatos_fecha]{detectar_formatos_fecha()}}
}
