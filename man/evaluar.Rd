% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluacion.R
\name{evaluar}
\alias{evaluar}
\title{Evaluar medidas, reglas y perfiles}
\usage{
evaluar(medicion, perfil)
}
\arguments{
\item{medicion}{\strong{Primer argumento.} Data frame producido por \code{medir()};
puede reunir varias corridas si conserva sus \code{id_medicion}. No es el
\code{perfil} descriptivo que devuelve \code{\link[=perfilar]{perfilar()}}.}

\item{perfil}{\strong{Segundo argumento.} Objeto creado por
\code{perfil_evaluacion()}, que reúne las reglas que se aplican a la medición.
Es un perfil de evaluación, distinto del objeto \code{perfil} creado por
\code{\link[=perfilar]{perfilar()}}.}
}
\value{
Objeto \code{evaluacion_calidad} con tres data frames filtrables:
\code{medidas}, \code{reglas} y \code{perfiles}. Si alguna regla declara un desenlace,
contiene además \code{desenlaces}, un plan que identifica las medidas
incumplidas, el valor medido, el motivo y la regla que lo produjo.
Cuando una métrica no pudo medirse, conserva \code{cobertura_metricas} y deja
en \code{NA} el resumen afectado, en lugar de tratar la ausencia como éxito.
Conserva además, en atributos, la configuración del modelo, la
aplicabilidad y el perfil de evaluación que produjo el resultado.
}
\description{
Ejecuta la cadena formal: condición por medida, proporción de medidas que
cumplen cada regla y media aritmética simple, no ponderada, de las reglas
del perfil. Los resultados por regla se conservan en \code{reglas}; el resumen de
\code{perfiles} no sustituye esa distribución.
}
\examples{
nucleo <- metricas_nucleo()
especifica <- especializar(nucleo$NoNulo)
instancia <- instanciar(especifica, "personas", "edad")
medidas <- medir(modelo(instancia), data.frame(edad = c(20, NA, 35)))
regla <- regla_evaluacion("Al menos 90\%", function(x) x > 0.9)
evaluar(medidas, perfil_evaluacion("Avanzado", regla))
}
