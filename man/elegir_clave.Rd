% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sugerir-clave.R
\name{elegir_clave}
\alias{elegir_clave}
\title{Elegir la clave entre las sugeridas}
\usage{
elegir_clave(datos, maximo = 5L, umbral_casi = 0.95)
}
\arguments{
\item{datos}{Tabla a examinar.}

\item{maximo}{Cuántas sugerencias devolver como máximo.}

\item{umbral_casi}{Proporción de valores distintos a partir de la cual una
columna que repite se informa igual, como candidata con duplicados.}
}
\value{
El nombre —o los nombres— de la clave elegida, o \code{NULL} si no se
eligió ninguna o la sesión no es interactiva.
}
\description{
Muestra las candidatas de \code{\link[=sugerir_clave]{sugerir_clave()}} numeradas, con el motivo de cada
una, y devuelve la que se elija para pasarla al argumento \code{clave} de
\code{\link[=perfilar]{perfilar()}}. La última opción permite escribir un nombre que no esté en la
lista, o varios separados por coma para una clave compuesta.
}
\details{
\strong{En una sesión no interactiva no pregunta}: devuelve \code{NULL} y avisa qué
habría ofrecido. Un guion que corre solo no puede quedarse esperando una
respuesta, y elegir por su cuenta sería exactamente lo que esta función
existe para no hacer.
}
\examples{
personas <- data.frame(id_persona = 1:3, edad = c(30, 41, 25))
# En una sesion interactiva pregunta; aqui devuelve NULL y dice que ofreceria.
elegir_clave(personas)

}
\seealso{
\code{\link[=sugerir_clave]{sugerir_clave()}}, y el argumento \code{clave} de \code{\link[=perfilar]{perfilar()}}.
}
