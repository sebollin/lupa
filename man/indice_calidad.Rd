% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tablero-calidad.R
\name{indice_calidad}
\alias{indice_calidad}
\title{Calcular un índice de calidad declarado por el usuario}
\usage{
indice_calidad(medidas, pesos, pesos_internos = NULL, ...)
}
\arguments{
\item{medidas}{Medición, tablero o análisis de \code{lupa}.}

\item{pesos}{Vector numérico nombrado por dimensión, en \verb{[0, 1]} y con
suma uno. Si se omite, se devuelve el tablero.}

\item{pesos_internos}{Vector opcional nombrado por \code{componente}; es
obligatorio para cada dimensión con más de una fila incluida.}

\item{...}{Argumentos enviados a \code{\link[=tablero_calidad]{tablero_calidad()}} cuando \code{medidas} no es
ya un tablero o análisis.}
}
\value{
Sin pesos, un \code{tablero_calidad}. Con pesos, un objeto S3
\code{indice_calidad} que nunca se imprime como un número aislado.
}
\description{
Sin \code{pesos}, devuelve \code{\link[=tablero_calidad]{tablero_calidad()}} y nunca un puntaje. Con pesos
nombrados por dimensión, transforma las métricas de defecto como
\code{1 - valor}, excluye las de orientación \code{no_aplica} y conserva cada paso.
}
\details{
Cuando una dimensión contiene varios componentes, \code{pesos_internos} debe
declarar una ponderación completa que sume uno dentro de esa dimensión.
No existe un promedio interno por omisión. El resultado conserva el tablero,
ambas capas de pesos, las inversiones, las exclusiones, los universos y la
cobertura del marco.
}
\examples{
nucleo <- metricas_nucleo()
instancias <- list(
  instanciar(especializar(nucleo$NoNulo), "padron", "codigo"),
  instanciar(especializar(nucleo$EntidadDuplicada), "padron")
)
medidas <- medir(modelo(instancias), data.frame(codigo = c("A", "B", "B")))
indice_calidad(medidas)
# Pesos propios de este ejemplo, no del paquete:
indice_calidad(
  medidas,
  pesos = c(Completitud = 0.6, Unicidad = 0.4)
)
}
\seealso{
\code{\link[=tablero_calidad]{tablero_calidad()}}
}
