% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deriva-perfil.R
\name{comparar_equivalencia}
\alias{comparar_equivalencia}
\title{Comparar la equivalencia de dos resúmenes de perfiles}
\usage{
comparar_equivalencia(anterior, actual, tolerancia)
}
\arguments{
\item{anterior, actual}{Un objeto \code{perfil} de \code{\link[=perfilar]{perfilar()}}, un objeto
\code{perfil_dbi} de \code{\link[=perfilar_dbi]{perfilar_dbi()}} o directamente un frame \code{columnas}.}

\item{tolerancia}{Número escalar no negativo y finito, declarado por quien
llama. No tiene valor por omisión y se publica en cada fila.

\strong{La tolerancia es mixta, no relativa.} La diferencia se divide por el
mayor de \code{1}, \verb{|anterior|} y \verb{|actual|}, que es la misma regla que usa la
detección de relaciones aritméticas: por debajo de magnitud 1 el divisor es
1 y la comparación pasa a ser absoluta, porque dividir por casi cero
convierte el ruido en un cambio del cien por ciento. Eso tiene una
consecuencia que conviene tener presente: una media que pasa de \verb{0,001} a
\verb{0,4} queda dentro de una tolerancia de \verb{0,5}. Cuando la escala usada fue
la unidad y no el valor, el \code{motivo} de esa fila lo dice
(\code{dentro_de_tolerancia_escala_unidad}, \code{fuera_de_tolerancia_escala_unidad})
y la columna publicada se llama \code{diferencia_normalizada}, no «relativa».}
}
\value{
Un frame de clase \code{equivalencia_perfiles} con \code{columna}, \code{campo},
\code{valor_anterior}, \code{valor_actual}, \code{diferencia_normalizada}, \code{veredicto},
\code{motivo}, \code{tipo_eje} y \code{tolerancia}. \code{veredicto} es un factor ordenado con
niveles \verb{identico < equivalente < materialmente_distinto}. Los atributos
\code{campos_no_comparables}, \code{detalle_campos_no_comparables},
\code{columnas_no_comparables}, \code{cobertura_diagnosticos}, \code{campos_protegidos}
y \code{resumen} declaran, respectivamente, los campos omitidos, los motivos
estructurales de esos campos, las columnas presentes en un solo lado o
con tipos incompatibles, los diagnósticos que no se pudieron evaluar, los
campos omitidos por protección y el conteo de cada veredicto.
\code{campos_protegidos} es un data frame con las columnas \code{columna}, \code{campo} y
\code{lado}; este último toma los valores \code{anterior} y \code{actual}.
}
\description{
Compara por intersección los campos registrados de dos perfiles y devuelve
una fila por cada par de columna y campo. Los campos que no tienen un eje
registrado no se comparan y quedan declarados en \code{campos_no_comparables}.
Los campos bajo protección tampoco se comparan: se declaran por columna,
campo y lado en \code{campos_protegidos}.
}
\details{
El registro fijo asigna tolerancia sólo a \code{media}, \code{mediana}, \code{desvio} y
\code{longitud_media}. Los conteos, proporciones de conteos y extremos por
selección se comparan en el eje \code{exacto}; las fechas canónicas en \code{fecha} y
\code{moda} y \code{centinela_valor} en \code{valor}. Los ejes \code{exacto}, \code{fecha} y \code{valor}
son binarios por construcción.

El comparador devuelve datos, no decisiones: no alimenta hallazgos,
severidades ni puntajes. La tolerancia es del llamador y jamás entra en una
regla del paquete; sólo se aplica al eje flotante finito y queda publicada
para que el llamador decida cómo usarla.

Si una columna es temporal en exactamente uno de los perfiles, los campos
de magnitud numérica se omiten por el cambio de esquema y su motivo queda en
\code{detalle_campos_no_comparables} como
\code{tipo_cambiado:temporal_vs_no_temporal}. Así no se comparan duraciones en
segundos contra magnitudes numéricas sin unidad común. Cuando ambos lados
son temporales, \code{desvio} sí se compara en flotante porque ambas puertas lo
expresan en segundos.

Si una columna guarda caracteres en exactamente uno de los perfiles, los
campos calculados sobre la representación —longitudes, variantes unicode y
números escritos como texto— se omiten y su motivo queda en
\code{detalle_campos_no_comparables} como \code{tipo_cambiado:texto_vs_no_texto}. Esos
campos cambian con sólo cambiar el almacenamiento, aunque el dato sea el
mismo, de modo que compararlos publicaría el síntoma y callaría la causa.
\code{factor} cuenta como almacenamiento de caracteres. Este guarda lee el tipo
\emph{declarado} y no el inferido: si el texto contiene números o fechas, la
inferencia borra justo la diferencia que hay que ver. Cuando el tipo
declarado viene de un vocabulario ajeno al de memoria —un tipo SQL, por
ejemplo— no se afirma nada y la comparación sigue como siempre.

Por la misma razón, si una columna lleva zona horaria en exactamente uno de
los perfiles, los campos que sólo ese almacenamiento puede medir se omiten
con el motivo \code{tipo_cambiado:con_zona_vs_sin_zona}. Del otro lado no dan un
valor distinto: no dan ninguno, porque no hay zona que comparar contra UTC.
Los extremos de fecha \strong{sí} se siguen comparando: al guardarse como texto
la columna pierde la zona y los instantes que denota son realmente otros,
que es lo más importante que hay para informar en ese caso.
}
\examples{
anterior <- data.frame(
  columna = "monto", media = 10, minimo = 1, moda = "a",
  stringsAsFactors = FALSE
)
actual <- data.frame(
  columna = "monto", media = 10.00000000001, minimo = 2, moda = "b",
  stringsAsFactors = FALSE
)
comparar_equivalencia(anterior, actual, tolerancia = 1e-9)
}
\seealso{
\code{\link[=comparar_perfiles]{comparar_perfiles()}}, \code{\link[=perfilar]{perfilar()}}, \code{\link[=perfilar_dbi]{perfilar_dbi()}}
}
