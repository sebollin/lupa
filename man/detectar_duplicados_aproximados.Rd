% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duplicados-aproximados.R
\name{detectar_duplicados_aproximados}
\alias{detectar_duplicados_aproximados}
\title{Detectar pares de filas con similitud aproximada}
\usage{
detectar_duplicados_aproximados(
  datos,
  columnas = NULL,
  metodo = "jw",
  umbral = 0.12,
  muestra = Inf,
  max_pares = 50000000L,
  max_resultados = 100L,
  normalizar = TRUE,
  perfil = NULL,
  proteger_datos_personales = TRUE,
  bloque = 1000L,
  estrategia = "auto",
  lsh_bandas = 12L,
  lsh_filas = 3L,
  lsh_q = 3L,
  lsh_max_cubeta = 1000L
)
}
\arguments{
\item{datos}{Tabla con una fila por entidad observada.}

\item{columnas}{Columnas atomicas a combinar. \code{NULL} aplica la seleccion
automatica descrita arriba; no se incluyen matrices ni listas.}

\item{metodo}{Medida admitida por \code{stringdist::stringdistmatrix()}. Por
defecto, \code{"jw"}.}

\item{umbral}{Distancia maxima para informar un par. Por defecto \code{0.12}.}

\item{muestra}{Máximo de filas candidatas. En el camino exacto queda sujeto
a \code{max_pares}; con LSH, \code{Inf} usa todas las filas.}

\item{max_pares}{Máximo de pares comparados en el camino exacto. Por defecto
\code{50000000}, que permite recorrer exhaustivamente hasta 10.000 filas con el
método y el bloque predeterminados; se puede reducir para limitar el tiempo.
En LSH el alcance se expresa con candidatos y cubetas, por lo que este
límite no se usa para recortar filas; el resultado lo marca explícitamente.}

\item{max_resultados}{Maximo de pares devueltos. Por defecto \code{100}.}

\item{normalizar}{Si se recortan espacios, se pasa a minusculas y se
colapsan espacios antes de calcular la distancia.}

\item{perfil}{Perfil de los mismos datos para reutilizar su clasificacion de
datos personales y no volver a inferirla.}

\item{proteger_datos_personales}{Si la evidencia de columnas protegidas se
reemplaza por \verb{[valor protegido]}. La supresion queda indicada en cada par.}

\item{bloque}{Cantidad de filas por tesela de comparación. Por defecto
\code{1000}; controla la memoria temporal, no el número de pares comparados.}

\item{estrategia}{Estrategia de comparación: \code{"auto"} (por omisión),
\code{"teselas"}, \code{"muestra"} o \code{"lsh"}. MinHash/LSH sólo se activa
automáticamente por encima del tope exhaustivo; se puede forzar con
\code{"lsh"}.}

\item{lsh_bandas}{Número de bandas del esquema LSH. Por defecto, 12.}

\item{lsh_filas}{Número de filas de firma por banda. Por defecto, 3.}

\item{lsh_q}{Longitud de los q-gramas usados para MinHash. Por defecto, 3.}

\item{lsh_max_cubeta}{Umbral a partir del cual una cubeta se considera
grande y se procesa por el mismo troceo acotado del camino exhaustivo.
No se descartan pares por este umbral; el alcance informa cuántas cubetas
y cuántos pares se procesaron de esta forma. Por defecto, 1000.}
}
\value{
Lista de clase \code{duplicados_aproximados} con \code{pares}, \code{hallazgos},
\code{alcance}, \code{columnas}, \code{metodo}, \code{umbral}, \code{disponible} y \code{razon}.
}
\description{
Compara filas seleccionadas de una tabla con \code{stringdist} y devuelve pares cuya
distancia esta bajo el umbral. El resultado describe similitud, distancia,
medida y alcance; nunca afirma que dos filas representen la misma entidad.
Por omision se combinan como maximo dos columnas de texto o factores, despues
de excluir nombres que parecen identificadores (\code{id}, \code{codigo}, \code{uuid}, entre
otros). Si quedan mas de dos columnas, la funcion pide indicar \code{columnas}
explicitamente en vez de mezclar campos que pueden diluir la similitud. La
medida predeterminada es Jaro--Winkler (\code{"jw"}), adecuada para transposiciones
y pequenas erratas; el umbral predeterminado \code{0.12} es deliberadamente mas
conservador que \code{0.15}, que sobre cadenas cortas y estructuradas produce
demasiados pares. Ambos argumentos se pueden cambiar.
}
\details{
Los pares con distancia cero se incluyen como \code{tipo_par = "exacto"}; los
restantes son \code{"aproximado"}. Ninguno demuestra identidad.

La comparacion usa teselas de \code{bloque} filas: cada matriz temporal se
descarta antes de continuar, por lo que la memoria no crece con el tamaño
de la tabla. El recorrido es exacto para las filas seleccionadas. \code{muestra}
y \code{max_pares} recortan el camino exacto; en el camino LSH, \code{muestra = Inf}
conserva todas las filas y \code{max_pares} no se aplica a las comparaciones.
En ese camino \code{alcance$limite_pares} es \code{NA}, mientras
\code{limite_pares_configurado} conserva el valor pedido como referencia y
\code{limite_pares_aplica} permite distinguir ambos casos sin inferirlo.
El objeto informa cuantos pares eran posibles, cuantos se compararon, el
modo, el tamaño de las teselas o los parámetros LSH y los que quedaron fuera. Solo se muestran
\code{max_resultados} coincidencias; el truncamiento tambien queda declarado.

\code{stringdist} es una dependencia opcional. Si no esta instalado, la funcion
devuelve un objeto con \code{disponible = FALSE}, una tabla vacia y el motivo
explicito; no falla ni presenta silencio como si se hubieran comparado todos
los pares.

Para tablas que superan el tope exhaustivo, \code{estrategia = "auto"} usa
MinHash con bandas LSH sobre todas las filas cuando \code{muestra = Inf}. La
salida declara las bandas, las filas por banda, las cubetas descartadas y la
probabilidad teorica de colision. Esa probabilidad se refiere al Jaccard de
los q-gramas, no a la medida final (\code{metodo}). \code{estrategia = "lsh"} fuerza
este camino; \code{"teselas"} y \code{"muestra"} conservan el camino exacto o
muestreado de las versiones anteriores. Las cubetas que superan
\code{lsh_max_cubeta} se procesan igualmente por lotes; el parámetro identifica
cubetas potencialmente costosas, pero no descarta sus pares. El alcance
separa \code{lsh_teselas_cubetas_grandes} (matrices del primer tramo) de
\code{lsh_lotes_cubetas_grandes} (lotes de filas de las bandas posteriores), pues
no son la misma unidad. Los pares aceptados por ambos recorridos entran al
resumen de Jaccard. Si alguna implementación futura descarta una cubeta,
la garantía se devuelve como \code{NA} y \code{lsh_garantia_estado} lo deja explícito.
La familia MinHash usa hashes afines inyectivos sobre un primo, con una
semilla interna fija, y restaura el estado global del generador de R; por eso
es determinista sin depender de \code{set.seed()}. La tabla de consulta de cada
hash evita repetir la aritmetica modular para cada celda de la matriz de
q-gramas. El diagnóstico de Jaccard puede quedar limitado a los primeros
pares del recorrido determinista; \code{lsh_jaccard_alcance} lo dice literalmente
y no presenta ese prefijo como una muestra representativa.
}
\examples{
datos <- data.frame(
  nombre = c("Ana Perez", "Ana Peres", "Luis Diaz"),
  domicilio = c("Calle 1", "Calle 1", "Calle 9")
)
pares <- detectar_duplicados_aproximados(datos)
if (!pares$disponible) pares$razon
}
\references{
Broder, A. Z. (1997). On the resemblance and containment of
documents. En \emph{Compression and Complexity of Sequences}, 21--29.
Leskovec, J., Rajaraman, A. y Ullman, J. D. (2020). \emph{Mining of Massive
Datasets} (3.ª ed.), capítulo 3.
}
\seealso{
\code{\link[=perfilar]{perfilar()}}, \code{\link[=reportar]{reportar()}}, \code{\link[=planificar_limpieza]{planificar_limpieza()}}
}
