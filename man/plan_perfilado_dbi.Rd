% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perfilar-dbi.R
\name{plan_perfilado_dbi}
\alias{plan_perfilado_dbi}
\title{Planificar el costo de \code{perfilar_dbi()} antes de pagarlo}
\usage{
plan_perfilado_dbi(
  conexion,
  tabla,
  universo = c("tabla_completa", "muestra_motor"),
  muestra_motor = NULL,
  muestra = Inf,
  orden_muestra = NULL,
  metricas = .METRICAS_DBI,
  estrategia_distintos = "exacta",
  estrategia_mediana = c("exacta", "aproximada_motor"),
  politica_costo = c("todas", "por_cardinalidad"),
  bloque_muestra = c("con_muestra", "solo_agregados"),
  max_consultas = Inf,
  dialecto = "auto",
  incluir_valores = TRUE,
  tamano_lote = NULL,
  tamano_lote_planos = .TAMANO_LOTE_PLANOS_DBI,
  tamano_lote_distintos = .TAMANO_LOTE_DISTINTOS_DBI,
  instrumentar = FALSE,
  umbral_cardinalidad = .UMBRAL_CARDINALIDAD_COSTO_DBI,
  max_celdas_muestra = .MAX_CELDAS_MUESTRA,
  max_bytes_muestra = .MAX_BYTES_MUESTRA,
  bloque_filas = NULL
)
}
\arguments{
\item{conexion}{Conexión abierta compatible con DBI.}

\item{tabla}{Nombre de tabla o un objeto aceptado por
\code{\link[DBI:dbQuoteIdentifier]{DBI::dbQuoteIdentifier()}}.}

\item{universo}{Universo sobre el que se calculan los agregados SQL:
\code{"tabla_completa"} (por omisión) o \code{"muestra_motor"}. En el segundo caso,
todas las métricas SQL usan la relación muestreada por el motor y no se
reemplazan silenciosamente por resultados de la tabla completa.}

\item{muestra_motor}{Cantidad positiva y finita de filas que el motor debe
tomar cuando \code{universo = "muestra_motor"}. Es obligatorio en ese universo y
debe quedar \code{NULL} para \code{"tabla_completa"}; la función rechaza temprano
valores no enteros, no positivos o \code{Inf}.}

\item{muestra}{Cantidad positiva de filas solicitadas para el perfil de
muestra que se trae a R, o \code{Inf} para traer la tabla entera. Este límite es
independiente de \code{universo}: en \code{muestra_motor}, \code{muestra_motor} decide las
filas del resumen SQL y \code{muestra} decide las filas del bloque
\code{perfil_muestra}. En \code{tabla_completa}, \code{muestra} no cambia los agregados.

Sin \code{orden_muestra}, las filas del bloque en R no son una muestra aleatoria
garantizada sino las primeras que devuelva el motor. El límite también
alcanza la muestra común con que se buscan dependencias. Use un entero
finito para acotar ese trabajo cuando el tiempo no sea la restricción.}

\item{orden_muestra}{Columnas para \verb{ORDER BY}. La salida solo declara orden
reproducible cuando la combinación es única en toda la tabla. Sin este
argumento, DBI no garantiza el orden ni la pertenencia de una muestra
limitada, y \code{meta} lo declara expresamente. No se usa cuando
\code{bloque_muestra = "solo_agregados"}.}

\item{bloque_filas}{En la via I1, entero positivo que activa la fuente por
\code{tabla_completa} y hace que el plan declare un unico result set
\code{dbSendQuery()} + \code{dbFetch(n = bloque_filas)}. \code{NULL} conserva el
plan historico; el plan no lee las filas y publica la proyeccion, el orden,
los bloques previstos y el costo declarado.}

\item{metricas}{Selección explícita de grupos de métricas: \code{"validos"},
\code{"distintos"}, \code{"moda"}, \code{"basicos"}, \code{"mediana"} y \code{"desvio"}. El
valor por omisión solicita las seis. Para traducir presets de versiones
anteriores: \code{seguro} equivale a
\code{c("validos", "basicos", "desvio")} y \code{conteos} equivale a
\code{"validos"}; \code{exacto} equivale a los valores por omisión de esta firma.}

\item{estrategia_distintos}{Procedencia explícita para \code{n_distintos}:
\code{"exacta"} (por omisión) emite \code{COUNT(DISTINCT)}; \code{"aproximada_motor"}
usa una función nativa aceptada por el motor y deja la métrica en
\code{no_disponible} si no existe; \code{"catalogo"} lee
\code{pg_stats.n_distinct} en PostgreSQL y publica el resultado como
\code{estimado_catalogo}, nunca como medición, cuando
\code{universo = "tabla_completa"}. En \code{muestra_motor} queda \code{no_disponible},
porque el catálogo describe la relación entera y la corrida mide un
subconjunto; y
\code{"omitida"} no emite ninguna consulta. No hay repliegue automático entre
estrategias. El resultado publica \code{estrategia_solicitada},
\code{estrategia_resuelta} y \code{estado} en \code{meta$estrategia_distintos}, y las dos
primeras también en \code{resumen_tabla$sql}. En \code{pg_stats}, un valor positivo
es el conteo estimado y uno negativo es una fracción de las filas. Cuando la
relación tiene descendientes se elige \code{inherited = TRUE}, porque esa fila
describe lo que lee una consulta sin \code{ONLY}; una relación sin hijas usa su
única fila propia. Las fracciones se convierten con la suma de
\code{pg_class.reltuples} de la jerarquía. Si no hay una fila utilizable —por
ejemplo, antes de \code{ANALYZE}— o hay ambigüedad, la métrica queda
\code{no_disponible}, no en cero.}

\item{estrategia_mediana}{Preferencia para resolver \code{mediana}: \code{"exacta"}
(por omisión) o \code{"aproximada_motor"}. La sonda prueba siempre primero una
forma nativa exacta consolidada, luego una forma exacta por columna y deja
las funciones nativas aproximadas para el final. Por eso esta opción
describe la estrategia habilitada, no garantiza el método ejecutado:
\code{meta$estrategia_mediana} y \code{resumen_tabla$sql$metodo} publican el método
que efectivamente corrió. Una mediana resuelta por una forma exacta queda
\code{estado = "calculado"} y \code{error_esperado = "no_aplica"}, aunque se haya
pedido \code{"aproximada_motor"}; sólo una aproximación ejecutada queda
\code{estado = "estimado"}. \code{universo = "muestra_motor"} combinado con
\code{"aproximada_motor"} se rechaza temprano.}

\item{politica_costo}{Política optativa para las métricas caras. El valor por
omisión, \code{"todas"}, conserva moda y mediana para todas las columnas
solicitadas. \code{"por_cardinalidad"} resuelve primero las fuentes
estructurales y mide valores válidos y distintos sólo cuando hace falta y
la estrategia lo permite. Luego omite, por columna, sólo la moda cuando la
proporción de distintos alcanza \code{umbral_cardinalidad}; la mediana se
conserva porque las mediciones disponibles muestran que su costo depende de
las filas y no de la cardinalidad. Los únicos valores aceptados son
\code{"todas"} y \code{"por_cardinalidad"}; no hay alias históricos.}

\item{bloque_muestra}{Qué bloques se solicitan: \code{"con_muestra"} (por
omisión) calcula también \code{perfil_muestra}, o \code{"solo_agregados"} omite su
lectura y devuelve sólo los agregados SQL. La segunda opción no cambia el
alcance de esos agregados: eso lo decide \code{universo}.}

\item{max_consultas}{Presupuesto declarado de consultas. Al agotarse, las
métricas restantes quedan en \code{no_disponible} con ese motivo.}

\item{dialecto}{Capacidad de acotar filas: \code{"auto"} la sondea, y
\code{"limit"}, \code{"top"}, \code{"fetch_first"}, \code{"rownum"} o \code{"portable"} la
declaran sin sondeo.}

\item{incluir_valores}{Si el resumen informa valores de celda: moda, mínimo,
máximo y mediana. Con \code{FALSE} esas consultas no se emiten.}

\item{tamano_lote}{Cantidad máxima de columnas por consulta consolidada.
Se conserva por compatibilidad y, si se informa, fija el tamaño de las dos
familias. Para control separado, usar \code{tamano_lote_planos} y
\code{tamano_lote_distintos}.}

\item{tamano_lote_planos}{Cantidad máxima de columnas por consulta de
agregados planos. El valor por omisión es 20.}

\item{tamano_lote_distintos}{Cantidad máxima de columnas por consulta de
cardinalidades exactas. El valor por omisión es 2, medido sobre el servidor
de referencia: el \verb{Shared Read} fue constante entre lotes y el costo por
columna fue casi igual para uno y dos, mientras el lote de dos derramó
menos que los lotes mayores. Una sola cardinalidad todavía puede forzar un
agregado pesado y derramar mucho más que un lote plano.}

\item{instrumentar}{En el plan, si es \code{TRUE}, cronometra las consultas de
preparación. No habilita consultas de datos ni agrega mediciones al objeto
devuelto: sus costos siguen siendo predicciones. Por omisión es \code{FALSE}.}

\item{umbral_cardinalidad}{Proporción entre valores distintos y válidos que
activa la omisión de la moda con \code{politica_costo = "por_cardinalidad"}. El
valor por omisión es \code{0.5} sólo cuando esa política se pide explícitamente;
se puede mover en cada llamada. Este argumento no gobierna la mediana:
\code{meta$decisiones_costo} explica la decisión de cada métrica por separado.
Para pedir todas las métricas use \code{politica_costo = "todas"}.}

\item{max_celdas_muestra}{Máximo de celdas que puede contener el bloque
\code{perfil_muestra}. Por defecto es \code{1000000}; se calcula antes de leer como
filas por columnas del esquema. Si reduce la muestra,
\code{perfil_muestra$cobertura_diagnosticos} usa la misma declaración que
\code{perfilar()}, con las celdas solicitadas, el umbral y el tope que mandó.
\code{Inf} desactiva este tope. No modifica los agregados SQL.}

\item{max_bytes_muestra}{Máximo de bytes de la muestra materializada que
alimenta \code{perfil_muestra}. Por defecto es \verb{512 MiB}. Como el tamaño no se
conoce desde el esquema, primero se lee una sonda de hasta cien filas y con
ella se fija el límite final en SQL o en \code{dbFetch(n)}, antes de leer el
resto. Si reduce la muestra, \code{cobertura_diagnosticos} informa los bytes
observados, el umbral y cuál tope mandó. \code{Inf} desactiva este tope. No
modifica los agregados SQL.}
}
\value{
Data frame de clase \code{plan_perfilado_dbi} con \code{clase_consulta},
\code{n_consultas}, \code{n_consultas_max} y \code{alcance}, y los atributos \code{total},
\code{total_minimo}, \code{total_maximo}, \code{total_lotes_rechazados}, \code{columnas},
\code{columnas_numericas}, \code{dialecto}, \code{consultas_emitidas}, \code{metricas},
\code{metricas_ejecucion}, \code{politica_costo}, \code{estrategia_distintos},
\code{fuente_cardinalidad_costo}, \code{moda_guardian}, \code{mediana_consolidada}, \code{filas},
\code{filas_fuente}, \code{estimacion_filas}, \code{proyecciones}, \code{mediana_escalar},
\code{tamano_lote_planos}, \code{tamano_lote_distintos}, \code{estimacion_derrame},
\code{estimacion_derrame_moda}, \code{estimacion_derrame_mediana},
\code{celdas}, \code{memoria_procesamiento}, \code{max_celdas_muestra},
\code{max_bytes_muestra}, \code{tope_muestra} y \code{muestreo}, y,
cuando se pide \code{distintos}, \code{supuesto_costo_distintos}.
\code{memoria_procesamiento} siempre tiene \code{estado = "no_estimada"}: no es una
estimación de consumo, sino la declaración de su ausencia, el motivo, la
magnitud del trabajo y referencias medidas de otras corridas. El atributo
\code{estimacion_derrame} es independiente: sólo describe la estimación del hash
en el motor para \code{COUNT(DISTINCT)} y no la memoria del procesamiento en R.
\code{estimacion_derrame_moda} y \code{estimacion_derrame_mediana} describen,
respectivamente, el nodo real de la moda y el sort de la mediana. Sus
lotes deciden por el máximo de columna; en una mediana consolidada,
\code{estado_io_total_bytes} es sólo la suma informativa de los tapes.
\code{muestreo} declara si la forma muestreada se pudo construir sin emitir una
consulta de datos. En \code{universo = "muestra_motor"}, cuando su \code{estado} es
\code{"no_disponible"}, el plan excluye las métricas SQL de esa muestra y
\code{supuesto} conserva el motivo.
Cuando se pide \code{bloque_muestra = "solo_agregados"}, también conserva ese
valor en el atributo \code{bloque_muestra} y no incluye la fila de la lectura de
muestra.

Los atributos \code{max_celdas_muestra}, \code{max_bytes_muestra} y \code{tope_muestra}
declaran la cota que se aplicará al bloque \code{perfil_muestra}. El plan no lee
datos: resuelve la cota de celdas con el ancho del esquema y declara que la
cota de bytes requiere una sonda de hasta cien filas durante la corrida.
Si la muestra pedida supera la cota de celdas, \code{tope_muestra} y \code{print()}
lo dicen antes de correr. La fila de consulta de muestra conserva su
conteo separado de los agregados; los topes no cambian ninguna consulta
SQL de resumen.

El costo no se declara como un número sino como un rango: \code{total} es el
extremo inferior, que supone que la política omite la moda cuya cardinalidad
no se conoce, y \code{total_maximo} el superior, que supone que la ejecuta. La
mediana queda fuera de ese supuesto proporcional y se cuenta según las
columnas numéricas solicitadas. Ambos incluyen la preparación y el perfilado; el rechazo
de lotes puede agregar las sondas de bisección declaradas por
\code{total_lotes_rechazados}. El costo real cae entre los extremos cuando
\code{universo} es \code{"tabla_completa"} o \code{attr(plan, "muestreo")$estado} es
\code{"disponible"}; en \code{"no_sondeado"} la forma sólo se construyó localmente
y el intervalo queda condicionado a que la sonda de la corrida la acepte.
Si la forma muestreada no se puede construir, el plan declara ese caso,
excluye sus métricas del rango y \code{attr(plan, "supuesto")} dice por qué.

Cuántas consultas se emiten no dice cuánto cuestan: catorce consultas
sobre dos millones de filas son mucho más trabajo que doscientas sobre
mil. Por eso el plan estima además la magnitud, y la estima en sus dos
mitades, porque el reloj de una corrida no lo decide siempre el motor.

La del motor va en \code{filas_leidas} (cuántas filas habría que leer) y
\code{ordenaciones_completas} (cuántas veces habría que ordenar la tabla
entera), y se resume en \code{magnitud_motor}. La del cliente va en
\code{columnas_texto} y \code{pares_texto} —cuántos pares de formas podría comparar
en R el detector de vocabulario sobre la muestra— y se resume en
\code{magnitud_texto}. \code{magnitud} es la mayor de las dos: \code{"baja"}, \code{"media"},
\code{"alta"}, o \code{"desconocida"} si no se conoce el número de filas.
\code{supuesto_costo} dice de dónde sale cada cuenta.

El plan previo no publica duraciones, CPU, ni filas o bytes medidos, ni agrega
una proyección temporal de \code{COUNT(DISTINCT)}. Puede publicar filas estimadas
por \code{pg_class.reltuples} y proyecciones de trabajo de moda/mediana, siempre
rotuladas como estimación de catálogo y no como medición. Aunque el plan de una corrida
conserve el atributo \code{supuesto_costo_distintos}, la medición y la proyección
sólo aparecen en \code{resumen_tabla$meta$costo_distintos}, después de ejecutar
el primer lote. El atributo sólo declara por qué esa proyección no existe
antes de correr; no es una duración ni una estimación temporal.

Si se pide \code{politica_costo = "por_cardinalidad"}, el plan busca primero una
garantía estructural o una fuente de catálogo. Si la fuente queda
desconocida, no emite un agregado para aclararla: \code{n_consultas} omite la
moda y \code{n_consultas_max} deja abierto el camino que la ejecuta. La mediana
se conserva porque su costo medido es plano frente a la cardinalidad y está
gobernado por las filas. La corrida mide \code{distintos} sólo si la política lo
necesita. La política por omisión es \code{"todas"}: el paquete no elige por el
usuario.
Una fuente estructural se resuelve aunque la estrategia de distintos este
omitida o no disponible; esta ultima solo gobierna si se puede medir.
El catalogo de la clave primaria se consulta siempre para conservar esa
respuesta en \code{resumen_tabla$meta$clave}; es una lectura de metadatos y no
un recorrido de la tabla.

Contar sólo el motor daba juicios falsos con números ciertos: una tabla de
3.912 filas con una columna de geometría en texto pedía 64.592 lecturas
—magnitud \code{"baja"}— y tardaba 35 segundos, porque el trabajo estaba en la
comparación de formas, que no es una lectura de fila. El método de
impresión muestra las dos mitades, avisa cuando la magnitud es alta y
nombra las palancas para acotarla, que no son las mismas de un lado que
del otro.
}
\description{
Emite sólo consultas de preparación —leer el esquema y sondear capacidades—
y devuelve cuántas consultas emitiría el perfilado completo, de qué clase y
con qué alcance sobre la tabla. No escanea datos para decidir el costo.
Cuando \code{politica_costo = "por_cardinalidad"}, una clave estructural exacta
puede cerrar la decisión; si no hay una fuente de catálogo utilizable, el
plan publica el rango entre omitir y ejecutar la moda; la mediana no entra en
ese criterio proporcional. Nunca lanza \verb{COUNT(DISTINCT ...)} para despejar
esa incertidumbre.
Las fuentes estructurales se resuelven cuando la política necesita la
cardinalidad, aunque \code{estrategia_distintos} no permita medirla. La
disponibilidad de la estrategia gobierna la medición, no el conocimiento que
ya da el catálogo.
Para el \code{COUNT(DISTINCT)} exacto, la preparación puede consultar además las
estadísticas de PostgreSQL (\code{pg_stats}, \code{pg_class.reltuples}, \verb{SHOW work_mem}
y, desde PostgreSQL 13, \verb{SHOW hash_mem_multiplier}) para estimar el tamaño
del hash y avisar un posible derrame. Esa consulta de metadatos no publica
cardinalidad medida ni reemplaza la medición posterior.
Para la moda y la mediana se publican, además, los atributos
\code{estimacion_derrame_moda} y \code{estimacion_derrame_mediana}. La moda deriva su
\code{metodo} (\code{"hash"} o \code{"sort"}) del primer \code{Aggregate} de un
\verb{EXPLAIN (FORMAT JSON, COSTS OFF)} de la consulta exacta, sin \code{ANALYZE} ni
lectura de datos. La mediana siempre modela un sort y distingue su huella de
decisión de la magnitud del tape. Ambas usan \code{n_validos} de catálogo en el
plan, pisos de 32 bytes para tipos fijos y 42 para \code{numeric}, y dejan el
objeto como \code{no_disponible} cuando el catálogo o el motor no permiten una
estimación.
Cuando esa lectura de catálogo trae \code{pg_class.reltuples} positivo, el plan lo
reutiliza como una estimación declarada del número de filas. La magnitud y las
proyecciones de trabajo de moda y mediana quedan entonces disponibles y dicen
explícitamente \code{estimado_catalogo}; no son duraciones ni mediciones. Un valor
cero o negativo —típico de una relación sin \code{ANALYZE}— conserva el estado
\verb{sin dato filas}. Los demás motores conservan ese estado si no tienen una
lectura de catálogo ya disponible.
}
\details{
\code{estrategia_distintos} declara la procedencia de \code{n_distintos} antes de la
corrida y conserva por separado lo pedido, lo resuelto y el estado. No hay
\code{auto}: \code{"exacta"} es el valor por omisión, \code{"aproximada_motor"} queda
\code{no_disponible} si el motor no ofrece una función aceptada, \code{"catalogo"}
lee \code{pg_stats.n_distinct} en PostgreSQL y publica una estimación con estado
\code{estimado_catalogo} cuando \code{universo = "tabla_completa"}, y \code{"omitida"} no
emite el agregado. Con \code{universo = "muestra_motor"}, \code{catalogo} queda
\code{no_disponible}: sus estadísticas describen la relación entera y no el
subconjunto de la corrida.
Un \code{n_distinct}
positivo es un conteo y uno negativo una fracción de las filas. Si la
relación tiene descendientes se usa la fila \code{inherited = TRUE}, que describe
la consulta sin \code{ONLY}; si no tiene hijas se usa la única fila propia. La
fracción se convierte con la suma de \code{pg_class.reltuples} de la jerarquía.
Si falta una estimación utilizable —por ejemplo, antes de \code{ANALYZE}— o hay
filas ambiguas, se conserva \code{no_disponible}, nunca cero.
\code{fuente_cardinalidad_costo} sigue siendo independiente y sólo describe el
número usado por la política de costo cuando esa política se pide.

El plan previo no proyecta segundos para \code{COUNT(DISTINCT)}: no lee los datos
y, por tanto, no tiene una referencia medida. La única referencia honesta es
el primer lote de distintos de la corrida, pero obtenerla cuesta una
consulta que este planificador no emite. Durante \code{perfilar_dbi()}, cuando hay
más de un lote y la instrumentación está activa, esa primera medición se
multiplica por la cantidad de lotes y se publica en
\code{resumen_tabla$meta$costo_distintos}. El aviso temporal llega después del
primer lote y antes del segundo; con un solo lote se declara que no hay nada
que proyectar.

La memoria del procesamiento no se estima: no escala de forma predecible con
las filas ni con las celdas, y eso se midió. El atributo
\code{memoria_procesamiento} conserva esa declaración, la magnitud conocida del
trabajo (\code{filas}, \code{celdas} y \code{pares_texto}) y referencias medidas de otras
corridas. Esas referencias no son una predicción para la tabla del plan:
traer costó aproximadamente 0,13 GB por millón de filas y procesar en R
aproximadamente 1,0-1,5 MB por cada mil filas, pero esta segunda cifra varió
1,62x entre tablas de la misma magnitud.

Ver todas las filas y tener todas las filas en memoria no son lo mismo. En
corridas de referencia, 4,5 millones de filas entraron en 0,6 GB y tardaron
25 segundos, mientras que procesar 4,5 millones ocupó aproximadamente 7 GB
y procesar 12,8 millones aproximadamente 19 GB. El problema observado está
en el procesamiento en R, no en la red ni en el motor.
}
\examples{
if (requireNamespace("DBI", quietly = TRUE) &&
    requireNamespace("RSQLite", quietly = TRUE)) {
  con <- DBI::dbConnect(RSQLite::SQLite(), ":memory:")
  DBI::dbWriteTable(con, "ejemplo", data.frame(id = 1:10, valor = 11:20))
  plan_perfilado_dbi(con, "ejemplo")
  DBI::dbDisconnect(con)
}
}
\seealso{
\code{\link[=perfilar_dbi]{perfilar_dbi()}}
}
