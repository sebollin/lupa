% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perfilar-dbi.R
\name{plan_perfilado_dbi}
\alias{plan_perfilado_dbi}
\title{Planificar el costo de \code{perfilar_dbi()} antes de pagarlo}
\usage{
plan_perfilado_dbi(
  conexion,
  tabla,
  muestra = 1000L,
  orden_muestra = NULL,
  modo = c("exacto", "seguro", "conteos", "muestreado", "aproximado"),
  metricas = NULL,
  max_consultas = Inf,
  dialecto = "auto",
  incluir_valores = TRUE,
  tamano_lote = .TAMANO_LOTE_DBI
)
}
\arguments{
\item{conexion}{Conexión abierta compatible con DBI.}

\item{tabla}{Nombre de tabla o un objeto aceptado por
\code{\link[DBI:dbQuoteIdentifier]{DBI::dbQuoteIdentifier()}}.}

\item{muestra}{Cantidad positiva y finita de filas solicitadas.}

\item{orden_muestra}{Columnas para \verb{ORDER BY}. La salida solo declara orden
reproducible cuando la combinación es única en toda la tabla. Sin este
argumento, DBI no garantiza el orden ni la pertenencia de una muestra
limitada, y \code{meta} lo declara expresamente.}

\item{modo}{Conjunto de métricas del resumen: \code{"exacto"} las calcula todas,
\code{"seguro"} evita las que ordenan o agrupan la tabla completa y
\code{"conteos"} deja solo el conteo de valores no nulos, \code{"muestreado"}
calcula estimaciones sobre filas elegidas por el motor y \code{"aproximado"}
usa funciones nativas aproximadas cuando la sonda las acepta.}

\item{metricas}{Selección explícita de grupos de métricas, que tiene
prioridad sobre \code{modo}: \code{"validos"}, \code{"distintos"}, \code{"moda"},
\code{"basicos"}, \code{"mediana"} y \code{"desvio"}.}

\item{max_consultas}{Presupuesto declarado de consultas. Al agotarse, las
métricas restantes quedan en \code{no_disponible} con ese motivo.}

\item{dialecto}{Capacidad de acotar filas: \code{"auto"} la sondea, y
\code{"limit"}, \code{"top"}, \code{"fetch_first"}, \code{"rownum"} o \code{"portable"} la
declaran sin sondeo.}

\item{incluir_valores}{Si el resumen informa valores de celda: moda, mínimo,
máximo y mediana. Con \code{FALSE} esas consultas no se emiten.}

\item{tamano_lote}{Cantidad máxima de columnas por consulta consolidada.
Veinte mantiene acotado el número de expresiones y se puede reducir para
motores con límites más estrictos.}
}
\value{
Data frame de clase \code{plan_perfilado_dbi} con \code{clase_consulta},
\code{n_consultas} y \code{alcance}, y los atributos \code{total}, \code{columnas},
\code{columnas_numericas}, \code{dialecto}, \code{consultas_emitidas}, \code{metricas},
\code{filas} y \code{tamano_lote}.

Cuántas consultas se emiten no dice cuánto cuestan: catorce consultas
sobre dos millones de filas son mucho más trabajo que doscientas sobre
mil. Por eso el plan estima además la magnitud, en los atributos
\code{filas_leidas} (cuántas filas habría que leer), \code{ordenaciones_completas}
(cuántas veces habría que ordenar la tabla entera), \code{magnitud} —\code{"baja"},
\code{"media"}, \code{"alta"} o \code{"desconocida"} si no se conoce el número de
filas— y \code{supuesto_costo}, que dice de dónde sale la cuenta. El método de
impresión avisa cuando la magnitud es alta y nombra las palancas para
acotarla.
}
\description{
Emite sólo las consultas-portón —contar filas, leer el esquema y sondear el
dialecto— y devuelve cuántas consultas emitiría el perfilado completo, de
qué clase y con qué alcance sobre la tabla. El plan hace visible cuántos lotes
de agregados se emitirán antes de empezar y evita una sorpresa de costo.
}
\examples{
if (requireNamespace("DBI", quietly = TRUE) &&
    requireNamespace("RSQLite", quietly = TRUE)) {
  con <- DBI::dbConnect(RSQLite::SQLite(), ":memory:")
  DBI::dbWriteTable(con, "ejemplo", data.frame(id = 1:10, valor = 11:20))
  plan_perfilado_dbi(con, "ejemplo")
  DBI::dbDisconnect(con)
}
}
\seealso{
\code{\link[=perfilar_dbi]{perfilar_dbi()}}
}
