% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duplicados-aproximados.R
\name{estimar_costo}
\alias{estimar_costo}
\title{Estimar el costo de una comparación de duplicados}
\usage{
estimar_costo(
  datos,
  columnas = NULL,
  metodo = "jw",
  umbral = 0.1,
  p = 0.1,
  muestra = Inf,
  max_pares = 50000000L,
  max_resultados = 100L,
  normalizar = NULL,
  perfil = NULL,
  proteger_datos_personales = TRUE,
  bloque = 1000L,
  estrategia = "auto",
  lsh_bandas = 12L,
  lsh_filas = 3L,
  lsh_q = 3L,
  lsh_max_cubeta = 1000L,
  lsh_muestra_estimacion = 400000L,
  presupuesto_pares = Inf,
  bloquear_por = NULL,
  lotes = FALSE,
  tamano_lote = 1000L,
  directorio_lotes = NULL,
  nucleos = getOption("lupa.nucleos", 2L)
)
}
\arguments{
\item{datos}{Tabla con una fila por entidad observada.}

\item{columnas}{Columnas atomicas a combinar. \code{NULL} aplica la seleccion
automatica descrita arriba; no se incluyen matrices ni listas.}

\item{metodo}{Medida admitida por \code{stringdist::stringdistmatrix()}. Por
defecto, \code{"jw"}.}

\item{umbral}{Distancia maxima para informar un par. Por defecto \code{0.10}.}

\item{p}{Factor de prefijo de Jaro--Winkler, entre 0 y 0.25. Por defecto
\code{0.1}; sólo tiene efecto con \code{metodo = "jw"}.}

\item{muestra}{Máximo de filas candidatas. En el camino exacto queda sujeto
a \code{max_pares}; con LSH, \code{Inf} usa todas las filas.}

\item{max_pares}{Máximo de pares comparados en el camino exacto. Por defecto
\code{50000000}, que permite recorrer exhaustivamente hasta 10.000 filas con el
método y el bloque predeterminados; se puede reducir para limitar el tiempo.
En LSH el alcance se expresa con candidatos y cubetas, por lo que este
límite no se usa para recortar filas; el resultado lo marca explícitamente.}

\item{max_resultados}{Maximo de pares devueltos. Por defecto \code{100}. Se
conservan los mas cercanos; entre pares empatados en distancia, el
desempate usa el orden canonico de los valores y no la posicion de las
filas, de modo que reordenar la tabla no cambia que pares sobreviven. Un
corte que cae dentro de un empate deja afuera pares igual de cercanos, y eso
se declara en \code{alcance$corte_en_empate}.}

\item{normalizar}{Perfil de comparación. \code{TRUE} conserva el perfil
predeterminado, \code{FALSE} desactiva sus pasos configurables, \code{"amplio"}
activa puntuación, ligaduras y ancho, y \code{\link[=normalizacion]{normalizacion()}} permite declarar
cada paso. Una lista nombrada puede resolver perfiles por columna. \code{NULL}
hereda el perfil guardado en \code{perfil}; si no se recibe uno, usa \code{TRUE}.
La normalización cambia sólo la representación usada para comparar, no los
datos guardados. El umbral se aplica sobre esa cadena normalizada. El
informe de fusiones sólo se calcula cuando algún paso configurable está
activo; con \code{FALSE} se omite. Si se entrega \code{perfil}, se reutiliza su
informe ya calculado.}

\item{perfil}{Perfil de los mismos datos para reutilizar su clasificacion de
datos personales y no volver a inferirla.}

\item{proteger_datos_personales}{Si la evidencia de columnas protegidas se
reemplaza por \verb{[valor protegido]}. La supresion queda indicada en cada par.}

\item{bloque}{Cantidad de filas por tesela de comparación. Por defecto
\code{1000}; controla la memoria temporal, no el número de pares comparados.}

\item{estrategia}{Estrategia de comparación: \code{"auto"} (por omisión),
\code{"teselas"}, \code{"muestra"} o \code{"lsh"}. MinHash/LSH sólo se activa
automáticamente por encima del tope exhaustivo; se puede forzar con
\code{"lsh"}.}

\item{lsh_bandas}{Número de bandas del esquema LSH. Por defecto, 12.}

\item{lsh_filas}{Número de filas de firma por banda. Por defecto, 3.}

\item{lsh_q}{Longitud de los q-gramas usados para MinHash. Por defecto, 3.}

\item{lsh_max_cubeta}{Umbral a partir del cual una cubeta se considera
grande y se procesa por el mismo troceo acotado del camino exhaustivo.
No se descartan pares por este umbral; el alcance informa cuántas cubetas
y cuántos pares se procesaron de esta forma. Por defecto, 1000.}

\item{lsh_muestra_estimacion}{Cantidad máxima de pares de filas usados para
estimar la proporción de candidatos, el tiempo del camino LSH y, si hay
\code{bloquear_por}, la pérdida de candidatos del bloqueo. La muestra es
interna, reproducible y su tamaño efectivo queda en \code{alcance}. Por defecto
se intentan 400.000 pares.}

\item{presupuesto_pares}{Se acepta por simetría de la firma y \strong{no aborta
aquí}: estimar el costo es justamente lo que se hace antes de fijar un
presupuesto, así que interrumpir la estimación por superarlo dejaría sin
respuesta la pregunta que motivó la llamada. Quien aborta es
\code{\link[=detectar_duplicados_aproximados]{detectar_duplicados_aproximados()}}, con el número que devuelve esta
función.}

\item{bloquear_por}{Nombre de una columna declarada por el usuario para
restringir la comparación a filas con la misma clave. La clave no tiene
significado incorporado en \code{lupa}; sus tamaños, ausentes y pares que
quedan fuera se registran en \code{alcance}. Los \code{NA} forman un bloque propio.}

\item{lotes}{Se acepta por simetría de la firma, pero la estimación no
escribe parciales ni modifica el directorio indicado.}

\item{tamano_lote}{Se acepta por simetría; no cambia el pronóstico.}

\item{directorio_lotes}{Se acepta por simetría y no se crea ni se usa al
estimar.}

\item{nucleos}{Cantidad máxima de hilos que \code{stringdist} puede usar. Por
defecto es \code{getOption("lupa.nucleos", 2L)}; \code{NULL} usa esa misma opción y
un valor mayor que los núcleos disponibles se limita de forma segura. El
resultado no depende de esta cantidad, pero el tiempo sí. El valor efectivo
queda declarado en \code{alcance$nucleos_usados}.}
}
\value{
Lista de clase \code{estimacion_costo_lupa} con los campos de la
estimación, \code{alcance}, \code{disponible} y \code{razon}.
}
\description{
Construye las firmas y calcula el pronóstico de candidatos sin recorrer las
cubetas ni comparar los pares. Es una operación deliberada: la medición del
reloj queda en el resultado de esta función y no es necesaria para obtener
\code{alcance} reproducible en \code{detectar_duplicados_aproximados()}.
}
\details{
En el camino LSH, \code{candidatos_previstos} es una estimación reproducible a
partir de una muestra de firmas. \code{tiempo_estimado_segundos} es un piso de la
medida aislada y no incluye firmas, cubetas ni troceo; sus campos de reloj
tienen \code{tiempo_determinista = FALSE}. Con \code{bloquear_por}, los pares entre
bloques no entran en el pronóstico y la pérdida estructural y estimada queda
en \code{alcance}. En el camino exacto, \code{candidatos_previstos} es la cantidad de
pares que se compararán, sujeta a \code{muestra}, \code{max_pares} y \code{bloquear_por}.

La función no escribe archivos ni modifica el estado del generador de R.
}
\examples{
datos <- data.frame(
  nombre = c("Ana Perez", "Ana Peres", "Luis Diaz"),
  grupo = c("A", "A", "B")
)
if (requireNamespace("stringdist", quietly = TRUE)) {
  costo <- estimar_costo(datos, columnas = "nombre", estrategia = "lsh")
  costo$candidatos_previstos
}
}
\seealso{
\code{\link[=detectar_duplicados_aproximados]{detectar_duplicados_aproximados()}}
}
