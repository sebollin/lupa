% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluacion.R
\name{propiedades_regla}
\alias{propiedades_regla}
\title{Propiedades declaradas de una regla de evaluación}
\usage{
propiedades_regla(regla)
}
\arguments{
\item{regla}{Objeto creado por \code{\link[=regla_evaluacion]{regla_evaluacion()}}.}
}
\value{
Data frame con una fila por propiedad: \code{propiedad}, \code{valor}.
}
\description{
Devuelve, en una tabla, lo que una regla declara: a qué métricas se engancha,
en qué nivel evalúa, qué desenlace produce y \strong{qué umbrales usa}. Los
umbrales viajan aparte de la condición justamente para esto: encerrados en el
\emph{closure} quedaban invisibles y obligaban a escribir otra regla para mover un
número.
}
\details{
Es la contraparte de \code{\link[=propiedades_metrica]{propiedades_metrica()}}, que describe métricas. Un
umbral pertenece a una regla, no a una métrica, así que no cabía allí.
}
\examples{
regla <- regla_evaluacion(
  "cobertura minima",
  function(x, minimo) x >= minimo,
  umbrales = list(minimo = 0.9)
)
propiedades_regla(regla)
}
\seealso{
\code{\link[=regla_evaluacion]{regla_evaluacion()}}, \code{\link[=propiedades_metrica]{propiedades_metrica()}}, \code{\link[=evaluar]{evaluar()}}
}
