% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analisis.R
\name{persistir_analisis}
\alias{persistir_analisis}
\alias{guardar_analisis}
\alias{leer_analisis}
\title{Guardar y recuperar un análisis}
\usage{
guardar_analisis(
  x,
  archivo,
  incluir_datos = FALSE,
  proteger_datos_personales = TRUE,
  sobrescribir = FALSE,
  comprimir = "xz"
)

leer_analisis(archivo)
}
\arguments{
\item{x}{Objeto creado por \code{\link[=analizar]{analizar()}}.}

\item{archivo}{Ruta del archivo RDS.}

\item{incluir_datos}{Si se persiste la copia de los datos conservada por
\code{analizar(conservar_datos = TRUE)}.}

\item{proteger_datos_personales}{Si se protege toda evidencia derivada.}

\item{sobrescribir}{Si se permite reemplazar un archivo existente.}

\item{comprimir}{Compresión admitida por \code{\link[=saveRDS]{saveRDS()}}: un lógico, \code{"gzip"},
\code{"bzip2"} o \code{"xz"}.}
}
\value{
\code{guardar_analisis()} devuelve la ruta de forma invisible;
\code{leer_analisis()} devuelve un objeto \code{analisis}.

El objeto guardado gana \code{meta$persistencia}, que declara qué se guardó y
qué no: \code{version_esquema}, si los datos viajan (\code{datos_incluidos}), si la
evidencia salió protegida (\code{evidencia_protegida}) y cuántas funciones se
sustituyeron por su descripción (\code{funciones_sustituidas}). Es el único
campo que un objeto leído tiene y el original no: lo que se guarda no es
idéntico a lo que se analizó, y esa diferencia se declara en vez de
suponerse.
}
\description{
Persiste un objeto \code{\link[=analizar]{analizar()}} en RDS con número de esquema. Los datos de
entrada no se guardan por omisión. Las funciones de reglas se sustituyen por
declaraciones pequeñas: una dependencia funcional se reconstruye al leer
sólo si los datos fueron incluidos; una función arbitraria queda desactivada.
Así el archivo no serializa entornos de ejecución completos.
}
\details{
La protección de datos personales con evidencia suficiente se vuelve a
aplicar antes de escribir. Incluir datos que contienen columnas protegidas
exige desactivar expresamente esa protección; una clasificación débil se
conserva como información pero no activa esa restricción.

El archivo se escribe con el \strong{formato RDS 2}, no con el 3 que \code{saveRDS()}
usa por omisión. El 3 anota la codificación nativa de quien escribe y al leer
\emph{traduce} desde ella el texto que no declara la suya; en Windows esa
traducción no falla y cambia los bytes en silencio, así que un análisis
guardado bajo un locale y leído bajo otro podía volver con el texto alterado.

Ese formato tiene un costo y conviene saberlo: \strong{no conserva la
representación compacta} de los vectores que R guarda así. Medido, una
columna \code{1:1e6} ocupa 103 bytes con el formato 3 y 2.127.903 con el 2; con
\code{incluir_datos = TRUE} sobre una tabla con una columna \code{id = seq_len(n)}, eso
son unos 2 MB por cada millón de filas, y a escala la escritura puede exigir
materializar en memoria lo que el formato 3 no materializa. Se eligió así
porque un archivo más grande es visible y recuperable, y un texto corrompido
en silencio no lo es. Quien prefiera la otra relación puede serializar el
objeto por su cuenta.
}
\examples{
a <- analizar(datos_administrativos, argumentos_perfil = list(
  analizar_dependencias = FALSE
))
ruta <- tempfile(fileext = ".rds")
guardar_analisis(a, ruta)
b <- leer_analisis(ruta)
unlink(ruta)
}
\seealso{
\code{\link[=analizar]{analizar()}}, \code{\link[=reportar]{reportar()}}, \code{\link[=guardar_historico]{guardar_historico()}}
}
