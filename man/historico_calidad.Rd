% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historico.R
\name{historico_calidad}
\alias{historico_calidad}
\alias{acumular_historico}
\title{Construir y ampliar un histórico de calidad}
\usage{
historico_calidad(..., detalle = c("resumen", "completo"))

acumular_historico(historico, ..., detalle = c("resumen", "completo"))
}
\arguments{
\item{...}{Objetos \code{medicion}, \code{evaluacion_calidad} o \code{historico_calidad}.
También puede darse una única lista que los contenga.}

\item{detalle}{Para evaluaciones, \code{"resumen"} conserva los niveles de regla y
perfil; \code{"completo"} conserva además cada evaluación de medida. Una
\code{medicion} pasada explícitamente siempre se conserva completa.}

\item{historico}{Objeto creado por \code{historico_calidad()}.}
}
\value{
Data frame S3 \code{historico_calidad}. La columna \code{version_esquema} y el
atributo del mismo nombre permiten migraciones futuras. \code{nivel} corresponde
a \code{medida}, \code{evaluacion_medida}, \code{evaluacion_regla} o
\code{evaluacion_perfil}.
}
\description{
Crea un data frame plano y versionado con corridas producidas por \code{\link[=medir]{medir()}} o
\code{\link[=evaluar]{evaluar()}}. \code{acumular_historico()} agrega objetos al mismo esquema y es
idempotente cuando recibe otra vez registros idénticos.
}
\details{
El detalle predeterminado evita repetir una fila por celda y regla cuando el
objetivo es monitorear la serie de evaluaciones. El objeto no guarda modelos,
closures, datos originales ni perfiles de profiling. Esto mantiene la tabla
exportable directamente con \code{write.csv()} o una herramienta de base de datos.

El esquema largo mapea las cuatro tablas de la sección 9.5 del marco mediante
\code{nivel}. Las columnas que no corresponden a un nivel quedan como \code{NA}.
}
\examples{
nucleo <- metricas_nucleo()
instancia <- instanciar(especializar(nucleo$NoNulo), "personas", "edad")
medidas <- medir(
  modelo(instancia), data.frame(edad = c(20, NA)),
  id_medicion = "enero", fecha = as.POSIXct("2026-01-31", tz = "UTC")
)
evaluacion <- evaluar(
  medidas,
  perfil_evaluacion("Basico", regla_evaluacion("Presente", function(x) x > 0))
)
historico_calidad(medidas, evaluacion)
}
\references{
AGESIC (2020). \emph{Marco de trabajo para la Gestión de la Calidad
de Datos en Gobierno Digital}, versión 1.6, sección 9.5, Presidencia de la
República, Uruguay.
}
