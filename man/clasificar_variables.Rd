% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/escalas-variables.R
\name{clasificar_variables}
\alias{clasificar_variables}
\title{Proponer escalas y roles de las variables}
\usage{
clasificar_variables(
  datos,
  perfil = NULL,
  metadatos = NULL,
  max_niveles = 100L,
  muestra = 1e+05,
  proteger_datos_personales = TRUE,
  columnas_personales = character()
)
}
\arguments{
\item{datos}{Tabla que se desea clasificar.}

\item{perfil}{Perfil opcional de los mismos datos.}

\item{metadatos}{Declaraciones opcionales por columna.}

\item{max_niveles}{Máximo de niveles guardados en cada lista.}

\item{muestra}{Máximo de valores usados para enumerar niveles no declarados;
los niveles ausentes de factores y metadatos se verifican sobre toda la
columna.}

\item{proteger_datos_personales}{Si se ocultan niveles concretos de columnas
cuya clasificación activa protección automática. Véase \code{\link[=perfilar]{perfilar()}}. Con
\code{perfil}, se respeta la clasificación de ese perfil; sin \code{perfil}, se
rehace aquí por léxico y por forma, y lo declarado entra por
\code{columnas_personales}.}

\item{columnas_personales}{Columnas que traen datos personales, declaradas
con la misma forma que acepta \code{\link[=perfilar]{perfilar()}}: nombres de columna, o un vector
con nombre donde el nombre es la columna y el valor es el tipo. \strong{Es el
único camino cuando no se pasa \code{perfil}}: sin perfil, la clasificación
corre por léxico y por forma, así que una columna que sólo quien la conoce
sabe personal —una edad, un legajo interno— se publicaría entera. Sólo
tiene efecto con \code{proteger_datos_personales = TRUE}.}
}
\value{
Data frame S3 \code{clasificacion_variables}, editable y filtrable.
}
\description{
Separa el tipo de almacenamiento, el tipo implícito y la escala de medición.
Una propuesta basada sólo en valores nunca queda confirmada: \verb{1, 2, 3} puede
ser conteo, código o nivel. Las clases \code{ordered}, \code{factor}, \code{logical}, las
clases temporales y el metadato \code{measure} sí constituyen declaraciones.
}
\details{
\code{metadatos} permite confirmar o corregir la propuesta con una tabla editable.
Debe contener \code{columna} y puede incluir \code{escala}, \code{rol}, \code{confianza},
\code{confirmada}, \code{unidad} y una columna de lista \code{niveles}. Las escalas válidas
son nominal, ordinal, discreta, continua, binaria, temporal y desconocida.

Los niveles declarados y observados se conservan como columnas de lista. Los
niveles ausentes son una observación, no prueba de error. Si la evidencia de
dato personal activa la protección, los niveles concretos se protegen.
}
\examples{
d <- data.frame(
  prioridad = ordered(c("baja", "alta"), levels = c("baja", "media", "alta")),
  cantidad = c(1L, 2L)
)
clasificar_variables(d)
}
\seealso{
\code{\link[=analizar]{analizar()}}, \code{\link[=distribucion_valores]{distribucion_valores()}}, \code{\link[=proponer_modelo]{proponer_modelo()}}
}
