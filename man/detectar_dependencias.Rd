% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependencias.R
\name{detectar_dependencias}
\alias{detectar_dependencias}
\title{Detectar dependencias funcionales entre columnas}
\usage{
detectar_dependencias(
  datos,
  umbral = 0.995,
  muestra = 1e+05,
  max_columnas = 100L,
  umbral_casi_constante = 0.95,
  umbral_casi_clave = 0.8,
  incluir_claves = FALSE,
  min_observaciones = 10L,
  max_ejemplos = 5L,
  max_comparaciones = 200000L,
  max_trabajo = 1e+08
)
}
\arguments{
\item{datos}{Tabla que se desea examinar.}

\item{umbral}{Cumplimiento mínimo en \verb{[0, 1]}.}

\item{muestra}{Máximo de filas; \code{Inf} desactiva el muestreo.}

\item{max_columnas}{Máximo de columnas analizadas.}

\item{umbral_casi_constante}{Proporción modal a partir de la cual un
determinante se descarta por casi constante.}

\item{umbral_casi_clave}{Tasa de valores distintos a partir de la cual un
determinante se descarta por casi clave, salvo que \code{incluir_claves} sea
verdadero.}

\item{incluir_claves}{Si se incluyen determinantes únicos, que satisfacen
dependencias de forma trivial.}

\item{min_observaciones}{Mínimo de pares presentes para informar una
dependencia.}

\item{max_ejemplos}{Máximo de contradicciones concretas en \code{evidencia}.}

\item{max_comparaciones}{Máximo de pares determinante-dependiente que se
comparan. \code{Inf} desactiva el presupuesto.}

\item{max_trabajo}{Máximo de unidades fila-par estimadas. \code{Inf} desactiva el
presupuesto que escala con las filas; se combina con
\code{max_comparaciones} y se aplica el límite más restrictivo.}
}
\value{
Data frame de clase \code{dependencias_funcionales}, ordenado por
cumplimiento y soporte. Los atributos \code{muestreado}, \code{filas_analizadas},
\code{columnas_analizadas}, \code{columnas_omitidas}, \code{columnas_descartadas} y
\code{truncado} documentan el alcance efectivo. \code{n_pares_posibles},
\code{n_pares_comparados}, \code{n_pares_sin_comparar} y \code{max_comparaciones}
documentan el presupuesto de comparaciones. \code{trabajo_estimado},
\code{trabajo_comparado}, \code{trabajo_sin_comparar}, \code{unidad_trabajo} y
\code{max_trabajo} documentan el presupuesto por filas. \code{columnas_descartadas}
es un data frame que explica por qué una columna no se usó como
determinante.
}
\description{
Busca pares ordenados \code{determinante -> dependiente}. El cumplimiento es la
proporción de filas que coincide con el valor modal del dependiente para
cada valor del determinante; por eso una dependencia aproximada señala
directamente las filas minoritarias que pueden ser errores de carga.
}
\details{
Para evitar resultados vacíos o triviales, se omiten por defecto las claves
únicas, los determinantes cuya tasa de valores distintos alcanza
\code{umbral_casi_clave}, los determinantes cuya moda alcanza
\code{umbral_casi_constante} y los dependientes constantes. El
valor predeterminado de \code{umbral = 0.995} exige que como máximo 5 de cada
1.000 filas contradigan la relación. Los ausentes de cualquiera de las dos
columnas no integran el cálculo.
El descarte ocurre antes de construir agrupaciones. El valor predeterminado
\code{umbral_casi_clave = 0.8} excluye determinantes con menos de 1,25 filas por
valor distinto en promedio: aun si cumplen, suelen describir una casi-clave
y no una regla reutilizable.

El costo crece aproximadamente como \code{columnas^2 * filas}. \code{max_columnas}
conserva las primeras columnas analizables, \code{max_comparaciones} limita el
número de pares columna a columna y \code{muestra} aplica una única muestra
sistemática a toda la tabla, de modo que las relaciones entre filas no se
rompen. \code{max_trabajo} acota el producto entre pares posibles y filas
analizadas. Es un presupuesto estimado, no una medición de segundos: permite
que el tope efectivo de pares baje cuando la tabla tiene muchas filas. Los
atributos del resultado declaran todos los recortes.
}
\examples{
datos <- data.frame(
  codigo = rep(1:3, each = 4),
  descripcion = rep(c("A", "B", "C"), each = 4),
  valor = seq_len(12)
)
detectar_dependencias(datos, min_observaciones = 4)
}
\seealso{
\code{\link[=detectar_claves]{detectar_claves()}}, \code{\link[=proponer_modelo]{proponer_modelo()}}, \code{\link[=perfilar]{perfilar()}}
}
