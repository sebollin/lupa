% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tablero-calidad.R
\name{tablero_calidad}
\alias{tablero_calidad}
\title{Construir un tablero de calidad}
\usage{
tablero_calidad(
  medidas,
  agregaciones = NULL,
  umbrales = NULL,
  marco = NULL,
  cobertura = NULL
)
}
\arguments{
\item{medidas}{Objeto creado por \code{\link[=medir]{medir()}} o \code{\link[=agregar]{agregar()}}.}

\item{agregaciones}{\code{NULL}, una agregación para todas las métricas, un
vector con nombres de métrica instanciada o un data frame con
\code{metrica_instanciada}, \code{agregacion} y, opcionalmente, \code{umbral}.}

\item{umbrales}{Vector numérico opcional con nombres de métrica
instanciada. Se exige para cada \code{ratio_umbral} que no lo declare dentro de
\code{agregaciones}.}

\item{marco}{Marco conceptual. Si se omite, usa el asociado a la medición,
el marco AGESIC cuando corresponde o el conjunto de factores medidos.}

\item{cobertura}{Cobertura opcional creada por \code{\link[=cobertura_analisis]{cobertura_analisis()}}.}
}
\value{
Data frame S3 \code{tablero_calidad}. Los atributos \code{alcance} y
\code{cobertura} conservan los conteos y el detalle del marco.
}
\description{
Resume una corrida en una fila por métrica y objeto. Las métricas
booleanas usan \code{ratio} por omisión y las reales usan \code{promedio}; la
elección queda siempre en la columna \code{agregacion}. \code{ratio_umbral} sólo se
aplica cuando se declara también el umbral correspondiente.
}
\details{
El objeto conserva la cobertura completa del marco: factores medidos, sin
métrica declarada, no aplicables y fuera de alcance. \code{\link[=print]{print()}} muestra
ambos elementos para que unas pocas filas nunca se lean como cobertura total.
}
\examples{
nucleo <- metricas_nucleo()
instancia <- instanciar(especializar(nucleo$NoNulo), "personas", "edad")
medidas <- medir(modelo(instancia), data.frame(edad = c(20, NA, 35)))
tablero_calidad(medidas)
}
\seealso{
\code{\link[=medir]{medir()}}, \code{\link[=agregar]{agregar()}}, \code{\link[=indice_calidad]{indice_calidad()}}
}
