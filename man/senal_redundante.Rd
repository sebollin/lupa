% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/senales-redundantes.R
\name{senal_redundante}
\alias{senal_redundante}
\title{Declarar una señal redundante entre columnas}
\usage{
senal_redundante(columnas, ventana = 0, transformacion = NULL, nombre = NULL)
}
\arguments{
\item{columnas}{Nombres de al menos dos columnas que codifican el mismo
hecho.}

\item{ventana}{Tolerancia máxima admitida entre los valores, en las unidades
del valor comparado. Por omisión \code{0}: coincidencia exacta.}

\item{transformacion}{Lista opcional con nombres de columna y funciones que
las llevan a una escala comparable.}

\item{nombre}{Etiqueta de la señal. Por omisión, las columnas unidas.}
}
\value{
Objeto de clase \code{senal_redundante}.
}
\description{
Declara que varias columnas de la misma tabla codifican \strong{el mismo hecho},
de modo que \code{\link[=detectar_discordancias]{detectar_discordancias()}} pueda informar las filas donde no
concuerdan. El grupo lo declara quien conoce los datos: \code{lupa} no lo adivina,
porque dos columnas de año pueden ser el año de nacimiento y el año de
ingreso y no tienen por qué coincidir.
}
\details{
\code{transformacion} permite comparar columnas que guardan el hecho de formas
distintas —por ejemplo extraer el año de una fecha para compararlo con una
columna de año—. Cada función recibe la columna entera y devuelve un vector
de la misma longitud.

\code{ventana} es la tolerancia, \strong{en las unidades del valor transformado}: con
años, \code{ventana = 1} acepta un año de diferencia. La unidad no se adivina; es
la del resultado de la transformación.
}
\examples{
senal_redundante(c("anio_fiscal", "anio_archivo"))
senal_redundante(
  c("fecha", "anio_fiscal"),
  transformacion = list(fecha = function(x) as.integer(format(x, "\%Y")))
)
}
\seealso{
\code{\link[=detectar_discordancias]{detectar_discordancias()}}, \code{\link[=detectar_dependencias]{detectar_dependencias()}}
}
