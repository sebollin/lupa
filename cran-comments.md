## R CMD check results

0 errors | 0 warnings | 2 notes

* This is a new submission.
* The local check environment does not provide the external `tidy` executable,
  so R skips HTML manual validation. Package examples, tests, vignettes, the PDF
  manual and the self-contained HTML generated by the package are checked
  independently.

## Test environments

* Local: R 4.6.1, x86_64-pc-linux-gnu, Pop!_OS 22.04 LTS.

## Reverse dependencies

This is the first release, so there are no reverse dependencies.
